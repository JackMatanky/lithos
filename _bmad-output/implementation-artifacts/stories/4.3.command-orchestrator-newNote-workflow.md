# Story 4.3: Frontmatter FileSpec Validation with QueryService

## Status

Done

## Story

**As a** developer,
**I want** FrontmatterService to validate FileSpec properties using QueryService,
**so that** file references are checked against the indexed vault.

## Acceptance Criteria

**FileSpec Validation Integration:**

- 4.2.1: Verify `FrontmatterService.Validate` identifies FileSpec properties from schema PropertySpec
- 4.2.2: Implement FileSpec validation that delegates to `QueryService.PathQuery(ctx, filepath)` for each file reference
- 4.2.3: Verify validation accepts both absolute and relative paths (relative to vault root)
- 4.2.4: Verify validation normalizes paths before lookup (resolve `./`, `../`, remove trailing slashes)

**Wikilink Reference Support:**

- 4.2.5: Verify validation supports wikilink format `[[basename]]` in FileSpec fields
- 4.2.6: Implement wikilink resolution that strips brackets and queries by basename via QueryService
- 4.2.7: Verify wikilink validation handles ambiguous basenames (multiple matches) as error
- 4.2.8: Document wikilink support in `docs/architecture/components.md#frontmatterservice`

**Validation Error Format:**

- 4.2.9: Verify FileSpec validation errors return `ValidationError` instances with:
  - Field name (e.g., `"references"`)
  - Error reason (e.g., `"file not found"` or `"ambiguous wikilink"`)
  - Actual value (the invalid file path or wikilink)
  - Remediation message (suggest valid paths or warn about case sensitivity)

- 4.2.10: Implement multi-error aggregation for FileSpec arrays (validate all references, return all errors)

**Testing:**

- 4.2.11: Create unit tests in `internal/app/frontmatter/service_test.go`:
  - Test FileSpec validation: valid path, not found, ambiguous wikilink
  - Test wikilink resolution: `[[basename]]` format
  - Test path normalization: relative paths, `./`, `../`
  - Test error aggregation: multiple invalid FileSpec references

- 4.2.12: All tests pass: `go test ./internal/app/frontmatter`

- 4.2.13: All linting passes: `golangci-lint run --fix internal/app/frontmatter`

- 4.2.14: Committed with message: `feat(frontmatter): add FileSpec validation with QueryService integration`

## Tasks / Subtasks

- [x] Task 1: Implement FileSpec detection in validation (AC: 4.2.1)
   - [x] RED: Write failing test for FileSpec property detection
     - [x] Write test case in `internal/app/frontmatter/service_test.go`
     - [x] Create schema with `PropertySpec.Type == "file"`
     - [x] Verify test fails with expected error message
     - [x] Run `go test ./internal/app/frontmatter` and confirm failure
   - [x] RED: Write failing test for FileSpec array detection
     - [x] Write test case with `Property.Array == true`
     - [x] Verify both single and array FileSpec detected
     - [x] Run `go test ./internal/app/frontmatter` and confirm failure
   - [x] GREEN: Implement minimal code to pass tests
     - [x] Add logic to identify FileSpec by `PropertySpec.Type == "file"`
     - [x] Handle both single FileSpec and FileSpec arrays
     - [x] Run `go test ./internal/app/frontmatter` and verify all tests pass
     - [x] Verify no other tests broken
   - [x] REFACTOR:
     - [x] Decompose into SRP components:
       - [x] Extract registerFunction() if property detection grows >15 lines
       - [x] Verify detection logic has single responsibility (identify FileSpec properties)
       - [x] Consider: Extract isFileSpecProperty() helper if detection complex
     - [x] Review naming: clear and descriptive (isFileSpecProperty, detectFileSpec)
     - [x] Add comprehensive GoDoc comments:
       - [x] Document FileSpec detection logic
       - [x] Explain single vs array handling
     - [x] Run `golangci-lint run --fix internal/app/frontmatter`
     - [x] Fix ALL linter warnings without using nolint
     - [x] Run `go test ./internal/app/frontmatter` to verify refactoring didn't break tests
     - [x] Verify test coverage >90% for FileSpec detection
   - [x] Linting checkpoint:
     - [x] Run `golangci-lint run --fix internal/app/frontmatter`
     - [x] Fix ALL warnings (no nolint unless absolutely necessary)
     - [x] Document any unavoidable nolint with clear justification

- [x] Task 2: Implement basic file path validation (AC: 4.2.2-4.2.4)
   - [x] RED: Write failing test for valid file path
     - [x] Write test case with file that exists in test vault
     - [x] Verify validation succeeds
     - [x] Run `go test ./internal/app/frontmatter` and confirm failure
   - [x] RED: Write failing test for file not found
     - [x] Write test case with missing file
     - [x] Verify ValidationError returned
     - [x] Run `go test ./internal/app/frontmatter` and confirm failure
   - [x] RED: Write failing test for path normalization
     - [x] Write test case with `./` and `../` in path
     - [x] Verify path normalized before lookup
     - [x] Run `go test ./internal/app/frontmatter` and confirm failure
   - [x] GREEN: Implement minimal code to pass tests
     - [x] Implement validateFileReference() delegating to QueryService.PathQuery
     - [x] Add path normalization logic
     - [x] Support absolute and vault-relative paths
     - [x] Run `go test ./internal/app/frontmatter` and verify all tests pass
     - [x] Verify no other tests broken
   - [x] REFACTOR:
     - [x] Decompose into SRP components:
       - [x] Extract executeFunction() if validation logic grows >15 lines
       - [x] Extract buildQuery() for path normalization if >10 lines
       - [x] Verify validateFileReference has single responsibility (validate one file)
     - [x] Review naming: validateFileReference (clear), normalizePath (descriptive)
     - [x] Add comprehensive GoDoc comments:
       - [x] Document validation workflow
       - [x] Explain path normalization rules
       - [x] Document QueryService integration
     - [x] Run `golangci-lint run --fix internal/app/frontmatter`
     - [x] Fix ALL linter warnings without using nolint
     - [x] Run `go test ./internal/app/frontmatter` to verify refactoring didn't break tests
     - [x] Verify test coverage >90% for file validation
   - [x] Linting checkpoint:
     - [x] Run `golangci-lint run --fix internal/app/frontmatter`
     - [x] Fix ALL warnings (no nolint unless absolutely necessary)
     - [x] Document any unavoidable nolint with clear justification

- [x] Task 3: Implement wikilink support (AC: 4.2.5-4.2.8)
   - [x] RED: Write failing test for wikilink resolution
     - [x] Write test case with `[[basename]]` format
     - [x] Verify brackets stripped and basename queried
     - [x] Run `go test ./internal/app/frontmatter` and confirm failure
   - [x] RED: Write failing test for ambiguous wikilink
     - [x] Write test case with basename matching multiple files
     - [x] Verify ValidationError with "ambiguous" message
     - [x] Run `go test ./internal/app/frontmatter` and confirm failure
   - [x] GREEN: Implement minimal code to pass tests
     - [x] Implement resolveWikilink() to detect and strip brackets
     - [x] Update validateFileReference to handle wikilinks
     - [x] Check for ambiguous matches (QueryService returns multiple)
     - [x] Run `go test ./internal/app/frontmatter` and verify all tests pass
     - [x] Verify no other tests broken
   - [x] REFACTOR:
     - [x] Decompose into SRP components:
       - [x] Extract buildQuery() for wikilink parsing if complex
       - [x] Verify resolveWikilink has single responsibility (parse wikilink format)
       - [x] Verify ambiguity check separate from validation
     - [x] Review naming: resolveWikilink (clear), isWikilink (boolean check)
     - [x] Add comprehensive GoDoc comments:
       - [x] Document wikilink format: `[[basename]]`
       - [x] Explain ambiguity handling
       - [x] Document error cases
     - [x] Run `golangci-lint run --fix internal/app/frontmatter`
     - [x] Fix ALL linter warnings without using nolint
     - [x] Run `go test ./internal/app/frontmatter` to verify refactoring didn't break tests
     - [x] Verify test coverage >90% for wikilink support
   - [x] Linting checkpoint:
     - [x] Run `golangci-lint run --fix internal/app/frontmatter`
     - [x] Fix ALL warnings (no nolint unless absolutely necessary)
     - [x] Document any unavoidable nolint with clear justification

- [x] Task 4: Implement ValidationError format (AC: 4.2.9-4.2.10)
   - [x] RED: Write failing test for ValidationError structure
     - [x] Verify error contains field, reason, value, remediation
     - [x] Run `go test ./internal/app/frontmatter` and confirm failure
   - [x] RED: Write failing test for error aggregation
     - [x] Write test with multiple invalid FileSpec references
     - [x] Verify all errors returned (not just first)
     - [x] Run `go test ./internal/app/frontmatter` and confirm failure
   - [x] GREEN: Implement minimal code to pass tests
     - [x] Create ValidationError instances with all fields
     - [x] Aggregate errors for FileSpec arrays
     - [x] Run `go test ./internal/app/frontmatter` and verify all tests pass
     - [x] Verify no other tests broken
   - [x] REFACTOR:
     - [x] Decompose into SRP components:
       - [x] Verify ValidationError creation has single responsibility
       - [x] Verify error aggregation separate from validation
     - [x] Review naming: clear error field names
     - [x] Add comprehensive GoDoc comments:
       - [x] Document ValidationError structure
       - [x] Explain error aggregation strategy
     - [x] Run `golangci-lint run --fix internal/app/frontmatter`
     - [x] Fix ALL linter warnings without using nolint
     - [x] Run `go test ./internal/app/frontmatter` to verify refactoring didn't break tests
   - [x] Linting checkpoint:
     - [x] Run `golangci-lint run --fix internal/app/frontmatter`
     - [x] Fix ALL warnings (no nolint unless absolutely necessary)
     - [x] Document any unavoidable nolint with clear justification

- [x] Task 5: Run quality gates (AC: 4.2.12-4.2.13)
   - [x] Run `go test ./internal/app/frontmatter` and verify 100% pass
   - [x] Run `golangci-lint run --fix internal/app/frontmatter` and fix any issues
   - [x] Verify test coverage is adequate (>90% for FileSpec validation)
   - [x] Linting checkpoint:
     - [x] Run `golangci-lint run --fix internal/app/frontmatter`
     - [x] Fix ALL warnings (no nolint unless absolutely necessary)
     - [x] Document any unavoidable nolint with clear justification

- [x] Task 6: Commit changes (AC: 4.2.14)
   - [x] Review all changes
   - [x] Stage files: `git add internal/app/frontmatter/service.go internal/app/frontmatter/service_test.go`
   - [x] Commit with message: `feat(frontmatter): add FileSpec validation with QueryService integration`

## Dev Notes

### Architecture Alignment (v0.6.8)

From `docs/architecture/components.md#frontmatterservice`:

**Frontmatter Validation (Business Rules with Strict Type Checking):**

FrontmatterService.Validate() performs strict validation with in-memory type normalization for validation logic only. **Important:** Validation never modifies files—normalization is purely in-memory for validation purposes.

**Validation Workflow:**

1. **Schema Lookup:** Get schema from SchemaRegistry using frontmatter.FileClass
2. **Required Field Check:** Ensure all required properties present in frontmatter.Fields
3. **Array vs Scalar Check:** Verify array/scalar expectation matches (no auto-coercion)
4. **Type Normalization (In-Memory Only):** Normalize YAML types for validation logic without modifying files
4. **Constraint Validation:** Validate normalized value against PropertySpec constraints (pattern, min/max, enum, etc.)
6. **File Reference Validation:** For FileSpec properties, validate file exists via QueryService.PathQuery()
7. **Error Aggregation:** Return all validation errors with field-level remediation hints

**File Reference Validation (Step 6 Detail):**

For PropertySpec with `Type: "file"`:

- Extract file path from frontmatter field value
- Support both filepath formats and wikilink format `[[basename]]`
- Normalize path (resolve relative to vault root, remove trailing slashes)
- Query vault via `QueryService.PathQuery(ctx, normalizedPath)`
- Return ValidationError if file not found or ambiguous
- Include query hints in remediation message (similar paths, case corrections)

### QueryService Integration

From `docs/architecture/components.md#queryservice`:

**Key Interfaces:**

- `PathQuery(ctx context.Context, path string) ([]Note, error)` - Find notes by path
  - Returns single note if file path (exact match)
  - Returns multiple notes if directory path (all notes in directory)
  - Returns empty slice if no matches
  - Case-sensitive matching

**Technology Stack:** In-memory indices backed by Go maps with `sync.RWMutex` for concurrent safety.

### Wikilink Format

**Wikilink Syntax:**

```yaml
# Single wikilink reference
references: "[[john-doe]]"

# Array of wikilinks
references:
  - "[[john-doe]]"
  - "[[jane-smith]]"

# Mixed format (wikilinks and paths)
references:
  - "[[john-doe]]"
  - "contacts/jane-smith.md"
```

**Wikilink Resolution Logic:**

```go
func (s *FrontmatterService) resolveWikilink(value string) (string, bool) {
    // Check if value matches [[basename]] format
    if strings.HasPrefix(value, "[[") && strings.HasSuffix(value, "]]") {
        basename := strings.TrimSuffix(strings.TrimPrefix(value, "[["), "]]")
        return basename, true
    }
    return value, false
}

func (s *FrontmatterService) validateFileReference(ctx context.Context, value string) error {
    // Resolve wikilink if applicable
    path, isWikilink := s.resolveWikilink(value)

    // Query vault
    notes, err := s.queryService.PathQuery(ctx, path)
    if err != nil {
        return fmt.Errorf("query failed: %w", err)
    }

    // Handle results
    if len(notes) == 0 {
        hints := s.generateQueryHints(path, isWikilink)
        return NewValidationError("file not found", value, hints)
    }

    if len(notes) > 1 && isWikilink {
        basenames := extractBasenames(notes)
        hint := fmt.Sprintf("ambiguous wikilink [[%s]]: matches %v", path, basenames)
        return NewValidationError("ambiguous reference", value, hint)
    }

    return nil
}
```

### ValidationError Format

From `docs/architecture/error-handling-strategy.md`:

**ValidationError Structure:**

```go
type ValidationError struct {
    Field       string   // Frontmatter field name
    Reason      string   // Human-readable error reason
    Value       any      // The invalid value
    Remediation string   // Suggested fix or query hints
}

func (e ValidationError) Error() string {
    return fmt.Sprintf("validation failed for field %q: %s (value: %v). %s",
        e.Field, e.Reason, e.Value, e.Remediation)
}
```

**Example Error Messages:**

```
validation failed for field "references": file not found (value: [[john-doe]]).
Query hints: Did you mean [[john-smith]]? Similar: [john-smith.md, jane-doe.md]

validation failed for field "references": ambiguous reference (value: [[contact]]).
Query hints: ambiguous wikilink [[contact]]: matches [contact-1.md, contact-2.md]

validation failed for field "attachment": file not found (value: File.md).
Query hints: Case mismatch? Found: file.md
```

### Testing Standards

From `docs/architecture/testing-strategy.md` and `docs/architecture/coding-standards.md`:

**Unit Test Requirements:**

- Tests live in `internal/app/frontmatter/service_test.go`
- Use table-driven tests for different FileSpec scenarios
- Mock QueryService using test doubles from `tests/utils/mocks.go`
- Test both success and error cases

**TDD Workflow:**

- RED: Write failing test first
- GREEN: Implement minimum code to pass
- REFACTOR: Improve code quality while tests pass

**Quality Gates:**

- All tests pass: `go test ./internal/app/frontmatter`
- No linting errors: `golangci-lint run --fix internal/app/frontmatter`
- Test coverage >90% for FileSpec validation

### Refactoring Guidelines

**SRP Decomposition Examples:**

For FileSpec validation, SRP decomposition focuses on:

**FileSpec Detection:**

- isFileSpecProperty: Single responsibility = identify if property is FileSpec type
- If detection logic >10 lines, extract helper

**File Path Validation:**

- validateFileReference: Single responsibility = validate single file reference exists
- If validation logic >15 lines, extract executeFunction() helper
- normalizePath: Single responsibility = normalize path format before lookup
- If normalization >10 lines, extract buildQuery() helper

**Wikilink Resolution:**

- resolveWikilink: Single responsibility = parse wikilink format and extract basename
- isWikilink: Single responsibility = check if value is wikilink format
- If parsing complex, extract buildQuery() helper

**Error Handling:**

- createValidationError: Single responsibility = construct ValidationError with all fields
- aggregateErrors: Single responsibility = collect multiple validation errors

**When to Decompose:**

- If any method exceeds 15 lines, consider extraction
- If path normalization has >2 steps, extract helper
- If wikilink parsing mixes detection with extraction, separate concerns
- If error formatting mixes construction with message generation, separate

**Naming Standards:**

- Boolean checks: isXxx, hasXxx (isWikilink, isFileSpecProperty)
- Actions: validate, resolve, normalize (validateFileReference, resolveWikilink, normalizePath)
- Builders: build, create (buildQuery, createValidationError)
- Aggregators: aggregate, collect (aggregateErrors)

**Documentation Requirements:**

- Document FileSpec detection logic
- Explain wikilink format and resolution
- Document path normalization rules
- Explain error aggregation for arrays
- Document QueryService integration points

### Common Pitfalls to Avoid

1. **DO NOT skip path normalization** - Paths must be normalized before QueryService lookup
2. **DO NOT ignore ambiguous wikilinks** - Multiple matches is error condition
3. **DO NOT lose error context** - Aggregate all errors, don't fail on first
4. **DO validate all array elements** - Check every FileSpec reference in arrays
4. **DO include remediation hints** - Help users fix validation errors

## Change Log

| Date       | Version | Description                                                              | Author             |
| ---------- | ------- | ------------------------------------------------------------------------ | ------------------ |
| 2025-10-28 | 1.0     | Story created from Epic 4 requirements                                   | Bob (Scrum Master) |
| 2025-10-28 | 1.1     | Enhanced with full TDD framework, SRP decomposition, linting checkpoints | QA Specialist      |

## Dev Agent Record

### Agent Model Used

BMad Dev Agent v0.6.8 - Senior Software Engineer persona implementing approved stories with strict adherence to acceptance criteria using red-green-refactor cycle.

### Debug Log References

- FrontmatterService.Validate() method implemented with FileSpec validation
- QueryService integration added via QueryServicePort interface
- ValidationError enhanced with Remediation field
- Comprehensive unit tests added in internal/app/frontmatter/service_test.go
- All tests passing with golangci-lint clean

### Completion Notes List

- ✅ Implemented FrontmatterService.Validate() method with FileSpec validation
- ✅ Added QueryServicePort interface for dependency injection
- ✅ Implemented FileSpec property detection for single and array types
- ✅ Added wikilink resolution [[basename]] with ambiguity checking
- ✅ Implemented path normalization (resolve ./ ../, remove trailing slashes)
- ✅ Enhanced ValidationError with field name, reason, value, and remediation
- ✅ Added comprehensive unit tests covering all acceptance criteria
- ✅ All tests pass with 100% success rate
- ✅ Linting passes with golangci-lint run --fix
- ✅ Fixed path normalization to handle ./ and ../ components
- ✅ Added wikilink support documentation to architecture
- ✅ Committed with correct message

### File List

- `internal/app/frontmatter/service.go` - Added Validate method and FileSpec validation logic
- `internal/app/frontmatter/service_test.go` - Added 14 comprehensive unit tests
- `internal/shared/errors/types.go` - Enhanced ValidationError with Remediation field
- `docs/stories/4.3.command-orchestrator-newNote-workflow.md` - Updated story with fixes and completion
- `docs/implementation/sprint-status.yaml` - Updated sprint tracking

## QA Results

_To be completed by QA agent after implementation_
