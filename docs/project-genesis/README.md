# Project Genesis

This folder contains initial ideation artifacts that led to the current project direction.

## Scope

- `project_brief.md`: the early framing of problem, users, and scope.
- `elicitation_summary.md`: discovery and reasoning that informed the brief.

## Status

Historical reference only. These files are not the authoritative source for current architecture or implementation rules.

For current decisions, use:

- `docs/adr/`
- Active context files (`CONTEXT-MAP.md` and module `CONTEXT.md` files)
- Current engineering guidance in `AGENTS.md`
