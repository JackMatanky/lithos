# Story 4.1: Implement Template Interface Pattern

## Status: Draft

## Story

**As a** system architect,
**I want** Template as domain interface with GoTemplate adapter wrapping *template.Template,
**so that** we achieve domain purity, testability, and proper hexagonal architecture separation.

## Acceptance Criteria

1. ✅ Create `/internal/domain/template.go` with Template interface:
   ```go
   type Template interface {
       ID() TemplateID
       Execute(data any) (string, error)
   }
   ```

2. ✅ Create `/internal/adapters/spi/template/go_template.go` with GoTemplate adapter:
   ```go
   type GoTemplate struct {
       id   TemplateID
       tmpl *template.Template
   }
   func (g *GoTemplate) Execute(data any) (string, error)
   ```

3. ✅ GoTemplate wraps `*template.Template` (standard library)
4. ✅ Template interface enables mock implementations for testing
5. ✅ TemplateID provides type-safe template identification
6. ✅ GoTemplateAdapter parses all templates with shared FuncMap
7. ✅ Domain services depend only on Template interface (not GoTemplate)
8. ✅ Unit tests use mock Template for pure domain testing
9. ✅ Integration tests use real GoTemplate adapter
10. ✅ All template usage migrated from direct *template.Template to Template interface

## Tasks / Subtasks

### TDD RED Phase: Write All Tests First

- [ ] Task 1: Write Unit Tests for Template Interface (TDD RED Phase - AC: 1-5)
  - [ ] Subtask 1.1: Write tests for Template interface contract (ID(), Execute())
  - [ ] Subtask 1.2: Write tests for TemplateID type-safety
  - [ ] Subtask 1.3: Write tests for GoTemplate construction
  - [ ] Subtask 1.4: Write tests for GoTemplate.Execute() wrapping *template.Template
  - [ ] Subtask 1.5: Write tests for mock Template implementations
  - [ ] Subtask 1.6: Run tests - Verify all FAIL (RED phase confirmation)

### TDD GREEN Phase: Implement Template Interface

- [ ] Task 2: Implement Template Interface and GoTemplate Adapter (TDD GREEN Phase - AC: 1-7)
  - [ ] Subtask 2.1: Create `/internal/domain/template.go` with Template interface
  - [ ] Subtask 2.2: Define TemplateID type in domain
  - [ ] Subtask 2.3: Create `/internal/adapters/spi/template/go_template.go`
  - [ ] Subtask 2.4: Implement GoTemplate struct wrapping *template.Template
  - [ ] Subtask 2.5: Implement GoTemplate.ID() method
  - [ ] Subtask 2.6: Implement GoTemplate.Execute() method with buffer handling
  - [ ] Subtask 2.7: Create GoTemplateAdapter for parsing templates with shared FuncMap
  - [ ] Subtask 2.8: Run tests - Verify all PASS (GREEN phase confirmation)
  - [ ] Subtask 2.9: Verify unit test coverage ≥85%

### TDD REFACTOR Phase: Improve Code Quality

- [ ] Task 3: Refactor Template Implementation (TDD REFACTOR Phase - AC: 1-7)
  - [ ] Subtask 3.1: Extract template parsing patterns
  - [ ] Subtask 3.2: Optimize buffer allocation in Execute()
  - [ ] Subtask 3.3: Improve error messages
  - [ ] Subtask 3.4: Run tests - Verify all PASS (refactoring safety)
  - [ ] Subtask 3.5: Run `golangci-lint run --fix` and resolve all warnings

### Migration & Integration

- [ ] Task 4: Migrate Existing Code to Template Interface (TDD Cycle - AC: 7-10)
  - [ ] Subtask 4.1: Write tests for all services using Template interface (not GoTemplate)
  - [ ] Subtask 4.2: Update CLICommander to use Template interface
  - [ ] Subtask 4.3: Update TemplateLoader to return Template interface
  - [ ] Subtask 4.4: Update all template consumers to use Template interface
  - [ ] Subtask 4.5: Remove all direct *template.Template references from domain/app layers
  - [ ] Subtask 4.6: Run tests - Verify all PASS
  - [ ] Subtask 4.7: Verify unit test coverage ≥85%

### Integration & Quality Assurance

- [ ] Task 5: Integration Testing and Documentation (AC: 8-10, All)
  - [ ] Subtask 5.1: Write integration tests with real GoTemplate adapter
  - [ ] Subtask 5.2: Write unit tests using mock Template implementations
  - [ ] Subtask 5.3: Run `just test-integration` and verify all tests pass
  - [ ] Subtask 5.4: Execute `pre-commit run --all-files`, stage changes, write conventional commit message
  - [ ] Subtask 5.5: Verify overall test coverage ≥85% for internal/domain and internal/adapters, ≥80% overall
  - [ ] Subtask 5.6: Update docs/architecture/data-models.md with Template interface
  - [ ] Subtask 5.7: Update docs/architecture/components.md with GoTemplate adapter
  - [ ] Subtask 5.8: Run final `just verify` to ensure all quality gates pass

## Dev Notes

### Priority: HIGH - Completes domain purity refactoring from architectural review
### Effort: 3 points
### Sprint: Sprint 5

### Dependencies
- Epic 3 Stories complete (domain entities enriched)

### Resolves
- Architectural Decision: Template Interface Pattern (Option C selected)
- Issue: Direct *template.Template dependency in domain layer

### Architecture Patterns

**Hexagonal Architecture (Ports & Adapters):**
Template is a domain interface, GoTemplate is the adapter:
- **Domain Layer:** Template interface (no stdlib dependencies)
- **Adapter Layer:** GoTemplate wraps *template.Template
- **Application Layer:** Depends only on Template interface

**Dependency Inversion:**
High-level services depend on Template abstraction, not concrete GoTemplate:
- **Testability:** Mock Template for pure domain tests
- **Flexibility:** Swap template engines without changing domain
- **Domain Purity:** No *template.Template in domain/app layers

**Wrapper Pattern:**
GoTemplate encapsulates *template.Template complexity:
- Buffer management hidden from consumers
- Error handling simplified
- Type-safe TemplateID

### Relevant Source Tree

**Files to Create:**
- `internal/domain/template.go` - Template interface and TemplateID
- `internal/adapters/spi/template/go_template.go` - GoTemplate adapter
- `internal/adapters/spi/template/go_template_test.go` - Adapter tests
- `internal/domain/template_test.go` - Interface contract tests

**Files to Modify:**
- `internal/app/cli/commander.go` - Use Template interface
- `internal/ports/spi/template_loader.go` - Return Template interface
- `cmd/lithos/main.go` - Wire GoTemplate adapter in DI

**Architecture Docs to Update:**
- `docs/architecture/data-models.md` - Template interface definition
- `docs/architecture/components.md` - GoTemplate adapter documentation

### Implementation Notes

**Phase 1: Interface Definition**
Create Template interface in domain layer. Clean, simple contract for template execution.

**Phase 2: Adapter Implementation**
Implement GoTemplate adapter wrapping *template.Template with buffer handling.

**Phase 3: Adapter Factory**
Create GoTemplateAdapter for parsing templates with shared FuncMap (enables template composition).

**Phase 4: Migration**
Update all template consumers to use Template interface instead of *template.Template.

**Phase 5: Testing**
Comprehensive unit tests (mock Template) and integration tests (real GoTemplate).

### Template Composition Support

**Shared FuncMap Pattern:**
```go
type GoTemplateAdapter struct {
    templates map[TemplateID]*template.Template
    funcMap   template.FuncMap
}

func NewGoTemplateAdapter(templateDir string, funcMap template.FuncMap) (*GoTemplateAdapter, error) {
    adapter := &GoTemplateAdapter{
        templates: make(map[TemplateID]*template.Template),
        funcMap:   funcMap,
    }

    // Parse all templates with shared function map
    baseTmpl := template.New("base").Funcs(funcMap)
    tmpl, err := baseTmpl.ParseGlob(filepath.Join(templateDir, "*.tmpl"))
    if err != nil {
        return nil, err
    }

    // Wrap each parsed template in GoTemplate
    for _, t := range tmpl.Templates() {
        id := TemplateID(t.Name())
        adapter.templates[id] = t
    }

    return adapter, nil
}
```

### Risks and Mitigations

**MEDIUM RISK: Template composition may break with wrapper**
- **Impact:** Templates calling other templates may not work
- **Mitigation:** Parse all templates together with shared FuncMap; test composition
- **Validation:** Integration tests with template composition ({{template "base" .}})

**LOW RISK: Performance overhead from wrapping**
- **Impact:** Buffer allocation on every Execute()
- **Mitigation:** Benchmark to ensure < 1ms overhead; acceptable for domain purity
- **Validation:** Performance tests comparing wrapped vs unwrapped execution

### Testing

**Test File Locations:**
Per coding-standards.md:
- `internal/domain/template_test.go` - Interface contract tests
- `internal/adapters/spi/template/go_template_test.go` - Adapter unit tests
- `tests/integration/template_test.go` - Integration tests with real adapter

**Testing Standards:**
From coding-standards.md:
- Table-driven tests for all branches
- Success and error paths
- Mock Template for domain tests
- Real GoTemplate for integration tests

**Testing Framework:**
- Go standard testing package
- testify/assert for assertions
- testify/mock for Template mocks

**Specific Test Requirements:**

1. **Interface Contract Tests:**
   - Template.ID() returns correct TemplateID
   - Template.Execute() returns rendered string
   - Template.Execute() handles errors correctly

2. **Adapter Unit Tests:**
   - GoTemplate wraps *template.Template correctly
   - Execute() uses buffer and returns string
   - Error handling preserves template errors

3. **Mock Template Tests:**
   - Domain services use mock Template for unit tests
   - No *template.Template in domain test code

4. **Integration Tests:**
   - Real GoTemplate renders templates correctly
   - Template composition works ({{template "other" .}})
   - FuncMap shared across all templates

5. **Migration Tests:**
   - All template consumers use Template interface
   - No direct *template.Template references in domain/app

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|---------|
| 2025-11-12 | 1.0 | Initial story creation from architectural review (Option C: Template as interface) | Sarah (PO) |

## Dev Agent Record

(To be populated during implementation)

## QA Results

(To be populated after QA review)
