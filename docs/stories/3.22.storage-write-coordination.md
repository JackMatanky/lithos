# Story 3.22: Implement Storage Write Coordination (Unit of Work)

## Status: Draft

## Story

**As a** system architect,
**I want** Unit of Work pattern for coordinated BoltDB + SQLite dual-write operations with transactional guarantees,
**so that** we prevent data inconsistency and ensure atomic writes across hybrid storage systems.

## Acceptance Criteria

**Unit of Work Pattern**:
1. ✅ Create `/internal/app/vault/unit_of_work.go`:
   ```go
   type CacheUnitOfWork struct {
       boltWriter   spi.CacheWriterPort
       sqliteWriter spi.CacheWriterPort
       operations   []operation
       mu           sync.Mutex
   }

   func (uow *CacheUnitOfWork) Begin() error
   func (uow *CacheUnitOfWork) AddWrite(note domain.Note, indexTime time.Time) error
   func (uow *CacheUnitOfWork) AddDelete(path string) error
   func (uow *CacheUnitOfWork) Commit(ctx context.Context) error
   func (uow *CacheUnitOfWork) Rollback(ctx context.Context) error
   ```

**Transaction Semantics**:
2. ✅ Batch operations collected during transaction (not immediate writes)
3. ✅ IndexTime consistency: Single indexTime parameter passed to AddWrite() is propagated to both BoltDB and SQLite writers (ensures identical timestamps across storages)
4. ✅ Commit sequence: BoltDB first, then SQLite (hot cache priority)
5. ✅ BoltDB write failure → entire transaction rollback (no SQLite write attempted)
6. ✅ SQLite write failure → rollback BoltDB changes (compensating transaction)
7. ✅ Transaction isolation - concurrent transactions don't interfere (mutex)

**Rollback Strategy**:
8. ✅ BoltDB rollback: Use `bolt.Tx.Rollback()`
9. ✅ SQLite rollback: Use SQL `ROLLBACK` statement
10. ✅ Compensating writes: If SQLite fails, delete from BoltDB what was written

**Integration**:
11. ✅ VaultIndexer uses CacheUnitOfWork for all cache writes
12. ✅ CLI commands wrap operations in UoW transactions
13. ✅ Error handling: Proper context, structured logging

**Testing**:
14. ✅ Unit tests: Begin/Commit/Rollback lifecycle
15. ✅ Integration test: Simulate SQLite write failure, verify BoltDB rollback
16. ✅ Integration test: Simulate BoltDB write failure, verify no SQLite write
17. ✅ Concurrency test: Parallel transactions don't interfere
18. ✅ Performance test: Transactional write overhead < 10% vs direct writes

## Tasks / Subtasks

### Feature 1: CacheUnitOfWork Core Implementation (TDD Cycle)

- [ ] Task 1: Write Unit Tests for CacheUnitOfWork (TDD RED Phase - AC: 1-2)
  - [ ] Subtask 1.1: Write tests for CacheUnitOfWork construction with both writers
  - [ ] Subtask 1.2: Write tests for Begin() transaction initialization
  - [ ] Subtask 1.3: Write tests for AddWrite() operation batching
  - [ ] Subtask 1.4: Write tests for AddDelete() operation batching
  - [ ] Subtask 1.5: Write tests for transaction isolation with mutex
  - [ ] Subtask 1.6: Run tests - Verify all FAIL (RED phase confirmation)

- [ ] Task 2: Implement CacheUnitOfWork (TDD GREEN Phase - AC: 1-3)
  - [ ] Subtask 2.1: Create `/internal/app/vault/unit_of_work.go`
  - [ ] Subtask 2.2: Define CacheUnitOfWork struct with both writers
  - [ ] Subtask 2.3: Define operation struct with indexTime field for WRITE operations
  - [ ] Subtask 2.4: Implement Begin() for transaction initialization
  - [ ] Subtask 2.5: Implement AddWrite(note, indexTime) to batch note writes with timestamp
  - [ ] Subtask 2.6: Implement AddDelete(path) to batch note deletions
  - [ ] Subtask 2.7: Add mutex for transaction isolation
  - [ ] Subtask 2.8: Run tests - Verify all PASS (GREEN phase confirmation)
  - [ ] Subtask 2.9: Verify unit test coverage ≥85%

- [ ] Task 3: Refactor CacheUnitOfWork (TDD REFACTOR Phase - AC: 1-2)
  - [ ] Subtask 3.1: Extract operation batching logic
  - [ ] Subtask 3.2: Optimize memory allocation for operation lists
  - [ ] Subtask 3.3: Improve error messages
  - [ ] Subtask 3.4: Run tests - Verify all PASS (refactoring safety)
  - [ ] Subtask 3.5: Run `golangci-lint run --fix` and resolve all warnings

### Feature 2: Two-Phase Commit Logic (TDD Cycle)

- [ ] Task 4: Write Unit Tests for Commit Logic (TDD RED Phase - AC: 3-5)
  - [ ] Subtask 4.1: Write tests for successful two-phase commit (BoltDB → SQLite)
  - [ ] Subtask 4.2: Write tests for BoltDB write failure (no SQLite write attempted)
  - [ ] Subtask 4.3: Write tests for SQLite write failure (compensating BoltDB rollback)
  - [ ] Subtask 4.4: Write tests for context cancellation during commit
  - [ ] Subtask 4.5: Run tests - Verify all FAIL (RED phase confirmation)

- [ ] Task 5: Implement Commit Logic (TDD GREEN Phase - AC: 3-6)
  - [ ] Subtask 5.1: Implement two-phase commit (BoltDB first, then SQLite) passing indexTime to both writers
  - [ ] Subtask 5.2: Ensure consistent indexTime across both storage systems (same timestamp for each operation)
  - [ ] Subtask 5.3: Handle BoltDB write failure (rollback entire transaction)
  - [ ] Subtask 5.4: Handle SQLite write failure (compensating BoltDB rollback)
  - [ ] Subtask 5.5: Add context cancellation support
  - [ ] Subtask 5.6: Add structured logging for commit phases
  - [ ] Subtask 5.7: Run tests - Verify all PASS (GREEN phase confirmation)
  - [ ] Subtask 5.8: Verify unit test coverage ≥85%

- [ ] Task 6: Refactor Commit Logic (TDD REFACTOR Phase - AC: 3-5)
  - [ ] Subtask 6.1: Extract commit phase logic into helper methods
  - [ ] Subtask 6.2: Optimize transaction error handling
  - [ ] Subtask 6.3: Improve logging context
  - [ ] Subtask 6.4: Run tests - Verify all PASS (refactoring safety)
  - [ ] Subtask 6.5: Run `golangci-lint run --fix` and resolve all warnings

### Feature 3: Rollback Strategy (TDD Cycle)

- [ ] Task 7: Write Unit Tests for Rollback (TDD RED Phase - AC: 6-9)
  - [ ] Subtask 7.1: Write tests for BoltDB rollback using bolt.Tx.Rollback()
  - [ ] Subtask 7.2: Write tests for SQLite rollback using SQL ROLLBACK
  - [ ] Subtask 7.3: Write tests for compensating writes (delete from BoltDB)
  - [ ] Subtask 7.4: Write tests for error wrapping per FR9
  - [ ] Subtask 7.5: Run tests - Verify all FAIL (RED phase confirmation)

- [ ] Task 8: Implement Rollback Strategy (TDD GREEN Phase - AC: 6-9)
  - [ ] Subtask 8.1: Implement BoltDB rollback using bolt.Tx.Rollback()
  - [ ] Subtask 8.2: Implement SQLite rollback using SQL ROLLBACK
  - [ ] Subtask 8.3: Implement compensating writes (delete from BoltDB on SQLite failure)
  - [ ] Subtask 8.4: Add error wrapping per FR9
  - [ ] Subtask 8.5: Add structured logging for rollback operations
  - [ ] Subtask 8.6: Run tests - Verify all PASS (GREEN phase confirmation)
  - [ ] Subtask 8.7: Verify unit test coverage ≥85%

- [ ] Task 9: Refactor Rollback Strategy (TDD REFACTOR Phase - AC: 6-9)
  - [ ] Subtask 9.1: Extract rollback logic into helper methods
  - [ ] Subtask 9.2: Optimize compensating transaction handling
  - [ ] Subtask 9.3: Improve error context and logging
  - [ ] Subtask 9.4: Run tests - Verify all PASS (refactoring safety)
  - [ ] Subtask 9.5: Run `golangci-lint run --fix` and resolve all warnings

### Feature 4: VaultIndexer Integration (TDD Cycle)

- [ ] Task 10: Write Unit Tests for VaultIndexer Integration (TDD RED Phase - AC: 10-12)
  - [ ] Subtask 10.1: Write tests for VaultIndexer with CacheUnitOfWork
  - [ ] Subtask 10.2: Write tests for cache write wrapping in UoW transactions
  - [ ] Subtask 10.3: Write tests for CLI command integration
  - [ ] Subtask 10.4: Write tests for error handling and context propagation
  - [ ] Subtask 10.5: Run tests - Verify all FAIL (RED phase confirmation)

- [ ] Task 11: Implement VaultIndexer Integration (TDD GREEN Phase - AC: 11-13)
  - [ ] Subtask 11.1: Update VaultIndexer constructor to accept CacheUnitOfWork
  - [ ] Subtask 11.2: Wrap VaultIndexer cache writes in UoW transactions
  - [ ] Subtask 11.3: Generate indexTime = time.Now() in VaultIndexer before calling AddWrite()
  - [ ] Subtask 11.4: Pass indexTime to UoW.AddWrite(note, indexTime) for consistent timestamps
  - [ ] Subtask 11.5: Update CLI commands to use UoW pattern
  - [ ] Subtask 11.6: Add proper error handling and context propagation
  - [ ] Subtask 11.7: Add structured logging for transaction lifecycle
  - [ ] Subtask 11.8: Run tests - Verify all PASS (GREEN phase confirmation)
  - [ ] Subtask 11.9: Verify unit test coverage ≥85%

- [ ] Task 12: Refactor VaultIndexer Integration (TDD REFACTOR Phase - AC: 10-12)
  - [ ] Subtask 12.1: Extract transaction coordination logic
  - [ ] Subtask 12.2: Optimize transaction batching
  - [ ] Subtask 12.3: Improve error messages
  - [ ] Subtask 12.4: Run tests - Verify all PASS (refactoring safety)
  - [ ] Subtask 12.5: Run `golangci-lint run --fix` and resolve all warnings

### Integration & Quality Assurance

- [ ] Task 13: Integration Testing (AC: 13-17)
  - [ ] Subtask 13.1: Write integration test for full UoW lifecycle (Begin → AddWrite → Commit)
  - [ ] Subtask 13.2: Write integration test simulating SQLite write failure with compensating rollback
  - [ ] Subtask 13.3: Write integration test simulating BoltDB write failure
  - [ ] Subtask 13.4: Write concurrency test with parallel transactions
  - [ ] Subtask 13.5: Write performance test measuring transaction overhead (target < 10%)
  - [ ] Subtask 13.6: Create `tests/integration/unit_of_work_test.go`
  - [ ] Subtask 13.7: Run `just test-integration` and verify all tests pass

- [ ] Task 14: Quality Assurance and Documentation (AC: All)
  - [ ] Subtask 14.1: Execute `pre-commit run --all-files`, stage changes, write conventional commit message
  - [ ] Subtask 14.2: Verify overall test coverage ≥85% for internal/app, ≥80% overall
  - [ ] Subtask 14.3: Document Unit of Work pattern in docs/architecture/patterns.md
  - [ ] Subtask 14.4: Document CacheUnitOfWork in docs/architecture/components.md
  - [ ] Subtask 14.5: Document transaction semantics and rollback strategy
  - [ ] Subtask 14.6: Add examples of UoW usage
  - [ ] Subtask 14.7: Run final `just verify` to ensure all quality gates pass

## Dev Notes

### Priority and Context
**Priority:** CRITICAL - Prevents data inconsistency
**Effort:** 5 points
**Sprint:** Sprint 3

This story implements the Unit of Work pattern to coordinate dual-writes across BoltDB (hot cache) and SQLite (deep storage), ensuring data consistency in the hybrid storage architecture.

### Incorporates Action Items
- Issue A6: Storage Write Coordination
- Gap 4.2: Partial resolution (transactional writes)

### Dependencies and Blockers

**Depends On:**
- Story 3.20: BoltDB adapters implemented
- Story 3.21: SQLite adapters implemented

**Blocks:**
- Story 3.30: Hybrid DI/E2E needs coordinated writes

**No Known Blockers**

### Architecture Patterns

**Unit of Work Pattern:**
The Unit of Work pattern ensures all-or-nothing writes across two storage systems:
- Batches operations during transaction
- Two-phase commit (BoltDB → SQLite)
- Rollback on any failure (compensating transactions)
- Transaction isolation with mutex

**Transaction Semantics:**
```
Begin() → Batching Phase
  ├─ AddWrite(note1, indexTime)
  ├─ AddWrite(note2, indexTime)
  └─ AddDelete(path)

Commit() → Two-Phase Commit
  ├─ Phase 1: Write to BoltDB
  │   ├─ Success → Continue to Phase 2
  │   └─ Failure → Rollback entire transaction
  │
  └─ Phase 2: Write to SQLite
      ├─ Success → Transaction complete
      └─ Failure → Compensating rollback (delete from BoltDB)
```

**Compensating Transaction Strategy:**
When SQLite write fails after successful BoltDB write:
1. Collect all successful BoltDB writes
2. Issue compensating DELETE operations to BoltDB
3. Verify compensating deletes succeeded
4. Return error with full context

**Concurrency Safety:**
- Mutex ensures single transaction at a time
- Read operations can proceed concurrently (no lock needed)
- Transaction isolation prevents partial writes

### Relevant Source Tree

**Files to Create:**
- `internal/app/vault/unit_of_work.go` - UoW implementation
- `internal/app/vault/unit_of_work_test.go` - Unit tests
- `tests/integration/unit_of_work_test.go` - Integration tests

**Files to Modify:**
- `internal/app/vault/indexer.go` - Use CacheUnitOfWork
- `cmd/lithos/commands/index.go` - Wrap operations in UoW

**Architecture Docs to Update:**
- `docs/architecture/patterns.md` - Add Unit of Work pattern
- `docs/architecture/components.md` - Document CacheUnitOfWork

### Implementation Notes

**Phase 1: Core UoW Implementation**
Create CacheUnitOfWork with Begin/AddWrite/AddDelete/Commit/Rollback methods.

**Phase 2: Two-Phase Commit**
Implement commit sequence: BoltDB first, then SQLite with proper error handling.

**Phase 3: Rollback Strategy**
Implement both direct rollback (abort before commit) and compensating rollback (undo after partial commit).

**Phase 4: Integration**
Update VaultIndexer and CLI commands to use UoW pattern.

**Phase 5: Testing**
Comprehensive testing of all transaction scenarios including failure modes.

### Operation Batching Design

```go
type operation struct {
    opType    operationType  // WRITE or DELETE
    note      domain.Note    // For WRITE operations
    indexTime time.Time      // For WRITE operations (when note was cached)
    path      string         // For DELETE operations (vault-relative path)
}

type operationType int
const (
    opWrite operationType = iota
    opDelete
)

// Batching during transaction
func (uow *CacheUnitOfWork) AddWrite(note domain.Note, indexTime time.Time) error {
    uow.mu.Lock()
    defer uow.mu.Unlock()

    uow.operations = append(uow.operations, operation{
        opType:    opWrite,
        note:      note,
        indexTime: indexTime,  // Store for commit phase
    })
    return nil
}

// Commit executes all batched operations
func (uow *CacheUnitOfWork) Commit(ctx context.Context) error {
    uow.mu.Lock()
    defer uow.mu.Unlock()

    // Phase 1: Write to BoltDB
    for _, op := range uow.operations {
        if op.opType == opWrite {
            // Pass indexTime to BoltDB writer for consistency
            uow.boltWriter.Write(ctx, op.note, op.indexTime)
        }
    }

    // Phase 2: Write to SQLite
    for _, op := range uow.operations {
        if op.opType == opWrite {
            // Pass SAME indexTime to SQLite writer for consistency
            uow.sqliteWriter.Write(ctx, op.note, op.indexTime)
        }
    }
}
```

### Risks and Mitigations

**HIGH RISK: Transaction rollback complexity (two storage systems)**
- **Impact:** Complex logic to undo partial writes across systems
- **Mitigation:** Comprehensive error handling; test all failure modes
- **Validation:** Integration tests for each failure scenario

**MEDIUM RISK: Performance impact of transactions**
- **Impact:** Batching and two-phase commit add overhead
- **Mitigation:** Benchmark to ensure < 10% overhead; acceptable for consistency
- **Validation:** Performance tests comparing UoW vs direct writes

**LOW RISK: Compensating transaction failures**
- **Impact:** What if compensating delete from BoltDB fails?
- **Mitigation:** Log critical error; retry logic; manual recovery documentation
- **Validation:** Test compensating transaction failure scenarios

### Testing

**Test File Locations:**
- `internal/app/vault/unit_of_work_test.go` - Unit tests
- `tests/integration/unit_of_work_test.go` - Integration tests

**Testing Standards:**
From coding-standards.md:
- Table-driven tests for all branches
- Success, validation failure, and cancellation paths
- Context cancellation testing

**Specific Test Requirements:**
1. **Unit Tests:** Begin/Commit/Rollback lifecycle, operation batching
2. **Failure Tests:** BoltDB write failure, SQLite write failure, compensating rollback
3. **Concurrency Tests:** Parallel transactions with mutex isolation
4. **Performance Tests:** Transaction overhead measurement (target < 10%)
5. **Integration Tests:** Full workflow with real BoltDB and SQLite adapters

### Performance Targets

**Transaction Overhead:**
- Batching overhead: < 1ms
- Two-phase commit overhead: < 5ms (both writes + coordination)
- Total overhead vs direct writes: < 10%

**Acceptable because:**
- Ensures data consistency (critical requirement)
- Prevents orphaned cache entries
- Enables reliable hybrid storage

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|---------|
| 2025-11-09 | 1.0 | Initial story creation from comprehensive architectural review | Sarah (PO) |
| 2025-11-13 | 1.1 | Validation fixes: Added indexTime parameter to AddWrite() signature (AC 1), added AC 3 for IndexTime consistency across both storages, renumbered ACs 3-18, updated Transaction Semantics example to use path instead of noteID and include indexTime, updated Operation Batching Design with indexTime field and Write() calls, updated subtasks to include IndexTime handling and propagation to both writers | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

(To be populated during implementation)

### Debug Log References

(To be populated during implementation)

### Completion Notes List

(To be populated during implementation)

### File List

(To be populated during implementation)

## QA Results

(To be populated after QA review)
