        if let Some((path, tags)) = old_data {
            // Continue with index cleanup
        } else {
            // New note (even though it's update call), add indexes
        }

        Ok(note)
    }
}
