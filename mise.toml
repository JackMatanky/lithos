# -----------------------------------------------------------------------------
# Filename:    lithos/mise.toml
# Docs:        https://mise.jdx.dev/
# Description: Project-wide tool versioning and task orchestration
# -----------------------------------------------------------------------------

[tools]
# Core Runtime
rust = { version = "nightly-2026-01-11", profile = "default" }
pre-commit = "latest"

# Development Utilities
"cargo:cargo-tarpaulin" = "latest"
"cargo:cargo-watch" = "latest"
"cargo:cargo-deny" = "latest"
"cargo:cargo-nextest" = "latest"
"cargo:wasm-pack" = "latest"

# ------------------------------------------------------------- #
#                           SETTINGS                            #
# ------------------------------------------------------------- #
[settings]
experimental = true
# Automate tool installation and management
auto_install = true
not_found_auto_install = true
fetch_remote_versions_cache = "1h"
fetch_remote_versions_timeout = "10s"
use_versions_host = true
trusted_config_paths = ["{{config_root}}"]
lockfile = true

# ------------------------------------------------------------- #
#                       ENVIRONMENT                             #
# ------------------------------------------------------------- #
[env]
# Test configuration for ADR 0010 test utilities integration
TEST_THREADS = "4"
CARGO_TEST_ARGS = "--lib --bins"
TEST_OUTPUT_DIR = "{{config_root}}/test-output"
CI = "${GITHUB_ACTIONS:-false}"

# Project paths (available for tasks)
PROJECT_ROOT = "{{config_root}}"
DOMAIN_CRATE = "{{vars.domain_crate}}"
APP_CRATE = "{{vars.app_crate}}"
ADAPTERS_CRATE = "{{vars.adapters_crate}}"
CLI_CRATE = "{{vars.cli_crate}}"

# ------------------------------------------------------------- #
#                        VARIABLES                              #
# ------------------------------------------------------------- #
[vars]
# Specific crate paths
domain_crate = "crates/domain"
app_crate = "crates/app"
adapters_crate = "crates/adapters"
cli_crate = "crates/cli"

# Test paths
tests_dir = "tests"
test_utils_crate = "{{vars.tests_dir}}/utils"
test_macros_crate = "{{vars.tests_dir}}/macros"

# Test Suite
test_suite_dir = "{{vars.tests_dir}}/suite"
test_arch_dir = "{{vars.test_suite_dir}}/arch"
test_e2e_dir = "{{vars.test_suite_dir}}/e2e"
test_integration_dir = "{{vars.test_suite_dir}}/integration"
test_bench_dir = "benches"

# Directory paths
docs_dir = "docs"
adr_dir = "docs/adr"

# Build and test outputs
target_dir = "target"
target_doc_dir = "target/doc"
nextest_dir = "target/nextest"

# Report files
coverage_report = "tarpaulin-report.html"
junit_unit = "nextest-unit.xml"
junit_integration = "nextest-integration.xml"

# Build artifacts
binary_name = "lithos"

# ------------------------------------------------------------- #
#                             Tasks                             #
# ------------------------------------------------------------- #

# -------------------------- Testing -------------------------- #
[tasks.test]
description = "Run all tests (unit, integration, e2e, and arch)"
alias = "t"
depends = ["test:unit", "test:integration", "test:e2e", "test:arch"]

# Specific crate unit tests for developer convenience
[tasks."test:unit:domain"]
description = "Run domain crate unit tests"
alias = "tud"
run = "mise run test:unit -- -p domain"
quiet = false

[tasks."test:unit:app"]
description = "Run app crate unit tests"
alias = "tuap"
run = "mise run test:unit -- -p app"
quiet = false

[tasks."test:unit:adapters"]
description = "Run adapters crate unit tests"
alias = "tuad"
run = "mise run test:unit -- -p adapters"
quiet = false

[tasks."test:unit:cli"]
description = "Run CLI crate unit tests"
alias = "tuc"
run = "mise run test:unit -- -p cli"
quiet = false

# Specific crate benchmarks for developer convenience
[tasks."test:bench:domain"]
description = "Run domain crate benchmarks"
alias = "tbd"
run = "mise run test:bench -- -p domain"
quiet = false

[tasks."test:bench:app"]
description = "Run app crate benchmarks"
alias = "tbap"
run = "mise run test:bench -- -p app"
quiet = false

[tasks."test:bench:adapters"]
description = "Run adapters crate benchmarks"
alias = "tbad"
run = "mise run test:bench -- -p adapters"
quiet = false

[tasks."test:bench:cli"]
description = "Run CLI crate benchmarks"
alias = "tbc"
run = "mise run test:bench -- -p cli"
quiet = false

# --------------------- CI/CD Simulation ---------------------- #
[tasks.ci]
description = "Simulate CI/CD pipeline"
depends = ["verify", "test:integration"]

[tasks.timing]
description = "Run verify with detailed timing information"
run = "mise run --timing verify"
shell = "bash"

# Hidden internal tasks (not shown in mise tasks)
[tasks._setup]
description = "Internal setup task"
run = "echo 'Setup complete'"
hide = true

[tasks._cleanup]
description = "Internal cleanup task"
run = "echo 'Cleanup complete'"
hide = true

# ------------------------- Cleanup --------------------------- #
# Note: Main clean task is defined in .mise/tasks/clean (file-based task)
# These are convenient aliases that call the main task with specific targets

[tasks."clean:cargo"]
description = "Clean only cargo build artifacts"
run = "mise run clean cargo"

[tasks."clean:test"]
description = "Clean only test output artifacts"
run = "mise run clean test"

[tasks."clean:reports"]
description = "Clean only coverage and JUnit reports"
run = "mise run clean reports"

# ----------------------- Quality Gates ----------------------- #
[tasks.quality]
description = "Run all quality gates (fmt, lint, adr:validate)"
alias = "q"
depends = ["lint", "adr:validate"]

[tasks.verify]
description = "Full quality gate orchestration"
alias = "v"
depends = ["lint", "test", "deny"]

[tasks.deny]
description = "Check dependencies for security vulnerabilities and license compliance"
run = "cargo deny check"
