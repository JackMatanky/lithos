{
  "name": "cal_year",
  "properties": {
    "type": {
      "required": false,
      "array": false,
      "type": "string",
      "enum": [
        "year"
      ]
    }
  },
  "extends": "cal",
  "excludes": [
    "date",
    "year_day",
    "quarter",
    "month_name",
    "month_number",
    "month_day",
    "week_number",
    "weekday_name",
    "weekday_number"
  ]
}
