---
title: 18-discovery-module-cleanup
category: enhancement
label: ready-for-agent
status: completed
date_created: 2026-06-18
date_updated: 2026-06-21
---

> *This was generated by AI during triage.*

## Type

AFK

## Labels

- root-config-discovery
- ready-for-agent

## Parent

- `.scratch/root-config-discovery/PRD.md`
- `.scratch/root-config-discovery/discovery-redesign-decisions.md`
- `docs/adr/discovery/0001-discovery-service-redesign.md`

## What to build

Remove the Phase-1 legacy code in `lithos-core/src/discovery/` that is now permanently orphaned following the typestate processor redesign (Issues 12–16). All items below carry `#[allow(dead_code, reason = "Phase-1 seam; ...")]` annotations or equivalent; none of them are reachable from any live call path.

Additionally, remove two stale `#[allow(dead_code)]` annotations on items that the Phase-1 annotations describe as future work but are in fact already wired into the live processor.

## Items to remove

### Entire files to delete

| File | Reason |
|------|--------|
| `lithos-core/src/discovery/engine.rs` | `DiscoveryEngine`, `DiscoveryInput`, `GlobalDiscoveryInput`, `GlobalDirectoryCandidate`, `VaultDiscoveryResult`, `GlobalDiscoveryResult`, `DiscoveredMarker` — all annotated Phase-1 seam; none called by `processor.rs` or `service.rs` |
| `lithos-core/src/discovery/diagnostics.rs` | `VaultDiscoveryWarning` — annotated Phase-1 seam; warnings have moved to `DiscoveryReport` |

### Symbols to remove from retained files

**`lithos-core/src/discovery/probe.rs`** — remove:
- `DiscoveryProbe<Output>` trait (Phase-1 seam)
- `VaultRootProbe` struct + `impl` (Phase-1 seam; superseded by `FolderProbe`)
- `GlobalRootProbe` struct + `impl` (Phase-1 seam; superseded by `FolderProbe`)
- `ROOT_MARKER_FILES` const (Phase-1/Phase-2 seam — if still only used by legacy probes)
- `GLOBAL_MARKER_FILES` const (Phase-1/Phase-2 seam — verify usage; remove if not referenced by live code)

**`lithos-core/src/discovery/walk.rs`** — remove:
- `DiscoveryBoundaries` struct (Phase-1 seam; only consumed by `DiscoveryEngine`)

**`lithos-core/src/discovery/selector.rs`** — remove:
- `select_candidate()` function (Phase-1 seam; only consumed by `DiscoveryEngine`)

**`lithos-core/src/discovery/policy.rs`** — remove:
- `DiscoveryPolicy` struct + `impl` (Phase-1 seam; consumed only by `DiscoveryEngine`)
- `VaultSourceType` enum (Phase-1 seam)
- `GlobalSourceType` enum (Phase-1 seam)
- Any `MarkerPattern` constants or items only used by the legacy engine or policy

### Stale `#[allow(dead_code)]` annotations to remove (items already live)

These two items are annotated as future work but are in fact already called from `processor.rs`:

| Item | File | Note |
|------|------|------|
| `FolderProbe` struct + `probe()` / `probe_dir()` methods | `probe.rs` | Called from `DiscoveryProcessor` — annotation is stale |
| `BoundedAscent` struct + iterator impl | `walk.rs` | Instantiated in `processor.rs` — annotation is stale |

Remove the `#[allow(dead_code, ...)]` attribute from each of these items only; do not remove the items themselves.

## Verification steps

After each deletion:

1. `cargo check --workspace` must compile without errors.
2. `mise run test` must pass with no test failures.
3. `mise run lint` must report no new warnings.

The compiler is the authoritative check for orphaned symbols — if removing a symbol causes a compile error, it is still used somewhere and must not be deleted in this issue.

## Files that must NOT be modified

- `lithos-core/src/discovery/processor.rs` — live typestate machine
- `lithos-core/src/discovery/service.rs` — live `DiscoveryService`
- `lithos-core/src/discovery/port.rs` — live `DiscoveryPort` trait
- `lithos-core/src/discovery/context.rs` — live `DiscoveryContext`
- `lithos-core/src/discovery/report.rs` — live `DiscoveryReport`
- `lithos-core/src/discovery/error.rs` — live `DiscoveryError`
- `lithos-core/src/discovery/mod.rs` — update `mod` declarations to remove deleted files; do not change any re-exports for live types
- `lithos-core/src/discovery/tests.rs` — live shared test utilities; remove only tests whose subject is a deleted symbol

## Acceptance criteria

- [ ] `engine.rs` is deleted; `mod engine;` is removed from `discovery/mod.rs`.
- [ ] `diagnostics.rs` is deleted; `mod diagnostics;` is removed from `discovery/mod.rs`.
- [ ] `DiscoveryProbe<Output>` trait, `VaultRootProbe`, and `GlobalRootProbe` are removed from `probe.rs`.
- [ ] `DiscoveryBoundaries` is removed from `walk.rs`.
- [ ] `select_candidate()` is removed from `selector.rs`; file may be deleted if it becomes empty.
- [ ] `DiscoveryPolicy`, `VaultSourceType`, and `GlobalSourceType` are removed from `policy.rs`; file may be deleted if it becomes empty.
- [ ] `FolderProbe` and `BoundedAscent` retain their stale `#[allow(dead_code)]` annotations removed — these items remain, only their suppression annotation is dropped.
- [ ] `cargo check --workspace` passes after all deletions.
- [ ] `mise run test` passes with no regressions.
- [ ] `mise run lint` passes with no new warnings.
- [ ] `mise run fmt` passes.
- [ ] No `#[allow(dead_code)]` remains in `discovery/` without a `reason` explaining why the suppression is still justified.

## Implementation notes

### Files deleted
- `lithos-core/src/discovery/engine.rs` — entire file (1247 lines incl. tests)
- `lithos-core/src/discovery/diagnostics.rs` — entire file (54 lines)
- `lithos-core/src/discovery/selector.rs` — emptied of production code, then deleted

### Files modified
- `lithos-core/src/discovery/probe.rs` — removed `DiscoveryProbe<Output>`, `VaultRootProbe`, `GlobalRootProbe`, `ROOT_MARKER_FILES`, `GLOBAL_MARKER_FILES`, `MarkerPattern`, `marker_from_path` + tests; removed stale `#[allow(dead_code)]` from `FolderProbe`
- `lithos-core/src/discovery/walk.rs` — removed `DiscoveryBoundaries` + tests; removed stale `#[allow(dead_code)]` from `BoundedAscent`
- `lithos-core/src/discovery/policy.rs` — removed `DiscoveryPolicy`, `VaultSourceType`, `GlobalSourceType`, `GlobalSourceDirectory` + tests; kept `MarkerPattern`, `VAULT_MARKER_PATTERNS`, `GLOBAL_MARKER_PATTERNS`, `BOUNDARY_MARKER_PATTERNS`
- `lithos-core/src/discovery/mod.rs` — removed `mod engine;`, `pub(super) mod diagnostics;`, `pub(super) mod selector;`
- `lithos-core/src/discovery/port.rs` — removed broken doc link to `DiscoveryEngine`
- `lithos-core/src/discovery/tests.rs` — emptied of engine-referencing tests (all 6 tests deleted)
- `lithos-core/src/discovery/error.rs` — removed orphaned `CurrentDirectoryCanonicalize` and `ReadDirectory` variants + tests (found during post-cleanup sweep — only consumed by deleted `engine.rs`)
- `lithos-core/tests/architecture.rs` — removed 2 obsolete tests checking deleted types
- `lithos-cli/src/main.rs` — replaced `CurrentDirectoryCanonicalize` with `CanonicalizePath`
- `lithos-cli/src/error.rs` — removed `ReadDirectory` match arm, doc ref, and test

### Verification
- `cargo check --workspace` — clean
- `cargo test --workspace` — 1962 unit + 48 integration + 42 e2e + doc tests: all pass
- `mise run lint` — clean
- `mise run fmt` — clean
- `gitnexus detect_changes` — LOW risk, 0 affected processes
- Zero `#[allow(dead_code)]` annotations remain in `discovery/`

## Out of scope

- Removing dead code outside `lithos-core/src/discovery/` (config diagnostics, template stubs, db foundations — tracked separately).
- Redesigning or extending the live discovery pipeline.
- Renaming or reorganizing live modules.
- Adding new functionality.

## Agent Brief

**Category:** enhancement

**Summary:** Delete permanently orphaned Phase-1 legacy symbols from `lithos-core/src/discovery/` and remove stale `#[allow(dead_code)]` annotations on items that are already wired into the live processor.

**Current behavior:**
The discovery module carries two full files (`engine.rs`, `diagnostics.rs`) and several symbols in `probe.rs`, `walk.rs`, `selector.rs`, and `policy.rs` that predate the typestate redesign. All are annotated as "Phase-1 seam" with `#[allow(dead_code)]` suppression. They are unreachable from any live call path. Two other items (`FolderProbe`, `BoundedAscent`) carry stale dead-code annotations despite already being called from `processor.rs`.

**Desired behavior:**
All Phase-1 seam items are deleted. `FolderProbe` and `BoundedAscent` have their now-incorrect `#[allow(dead_code)]` annotations removed. The module compiles clean with no dead-code suppressions that lack justification. Test suite passes without regressions.

**Key interfaces:**
- `DiscoveryEngine` — delete entirely (lives in `engine.rs`)
- `VaultDiscoveryWarning` — delete entirely (lives in `diagnostics.rs`)
- `DiscoveryProbe<Output>` trait, `VaultRootProbe`, `GlobalRootProbe` — delete from `probe.rs`
- `DiscoveryBoundaries` — delete from `walk.rs`
- `select_candidate()` — delete from `selector.rs`
- `DiscoveryPolicy`, `VaultSourceType`, `GlobalSourceType` — delete from `policy.rs`
- `FolderProbe`, `BoundedAscent` — retain; remove their `#[allow(dead_code)]` attributes only

**Acceptance criteria:**
- [ ] `engine.rs` and `diagnostics.rs` deleted; `mod` declarations removed from `discovery/mod.rs`
- [ ] All named Phase-1 symbols deleted from retained files
- [ ] `FolderProbe` and `BoundedAscent` `#[allow(dead_code)]` annotations removed (items kept)
- [ ] `cargo check --workspace` clean, all tests pass, lint clean
- [ ] No unjustified `#[allow(dead_code)]` remains in `discovery/`

**Out of scope:**
- Dead code outside `discovery/`
- Live pipeline changes
- New functionality

## Blocked by

- `.scratch/root-config-discovery/16-cli-discovery-subcommands.md` (merged ✓)
