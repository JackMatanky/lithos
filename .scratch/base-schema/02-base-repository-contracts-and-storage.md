---
title: 02-base-repository-contracts-and-storage
category: enhancement
label: ready-for-agent
status: closed
date_completed: 2026-06-02
date_created: 2026-06-01
---

## Type

AFK

## Labels

- base-schema
- ready-for-agent

## Parent

- `.scratch/base-schema/PRD.md`

## What to build

Add persistence seams for base schemas across repository traits and storage adapters so the processor can save/load base state by `SchemaId`.

This slice wires the read/write contract end-to-end for `BaseSchema` in repository traits, in-memory test repository, and redb table definitions.

## Acceptance criteria

- [ ] `ReadRepository`/`WriteRepository` expose `find_base_schema_by_id`, `find_base_schemas_by_ids`, `save_base_schema`, and `delete_base_schema`.
- [ ] `lithos-core/src/schema/storage/tables.rs` defines `BASE_SCHEMA_BY_ID` table.
- [ ] `lithos-core/src/schema/storage/read.rs` implements base-schema read methods.
- [ ] `lithos-core/src/schema/storage/write.rs` implements base-schema write/delete methods.
- [ ] `InMemoryRepository` supports the same base-schema methods for tests.
- [ ] InMemoryRepository round-trip tests (in `testing.rs`) prove save/find/delete behavior and deterministic ID lookups.
- [ ] RedbRepository round-trip tests (in `read.rs`/`write.rs`) prove save/find/delete behavior with persistent storage.

## Blocked by

- `.scratch/base-schema/01-base-domain-and-deltas.md`

## Agent Brief

> *This was generated by AI during triage. Updated 2026-06-02 per review.*

**Recommendation**: keep `ready-for-agent`.

**Primary contracts (types, not files):**
- `schema::repository::ReadRepository` — add `find_base_schema_by_id(SchemaId)` returning `Result<Option<BaseSchema>>` and `find_base_schemas_by_ids(&[SchemaId])` returning `Result<Vec<BaseSchema>>`
- `schema::repository::WriteRepository` — add `save_base_schema(&BaseSchema)` and `delete_base_schema(SchemaId)`, both returning `Result<()>`
- If easy to implement, `save_many_base_schemas(&[BaseSchema])` on `WriteRepository` (`Result<()>`)

**Storage:**
- Define `BASE_SCHEMA_BY_ID` as `UuidTable<SchemaId, &[u8]>` (same pattern as `SCHEMAS`)
- Implement reads in `ReadRepository for RedbRepository`
- Implement writes in `WriteRepository for RedbRepository`
- Extend `InMemoryRepository` with `HashMap<SchemaId, BaseSchema>` field + impl blocks

**Constraints:**
- Keep trait contracts explicit and fallible (`Result<...>`), no panic paths
- Preserve deterministic ordering for `find_*_by_ids` results (input order)
- `BaseSchema` already derives `rkyv` archival — no new serialization code needed
- Follow existing `try_open_table` + `from_bytes` pattern for RedbRepository reads
- `BASE_SCHEMA_BY_ID` is a simple ID-keyed table — no cross-table index (unlike `save_schema` which updates name index)
- `delete_base_schema` is idempotent (missing IDs are OK)

**Verification checklist:**
- [ ] `InMemoryRepository` round-trip tests in `testing.rs` | `#[cfg(test)] mod base_schema`
- [ ] `RedbRepository` round-trip tests in `read.rs` / `write.rs` | `#[cfg(test)] mod base_schema`
- [ ] Missing-ID returns `None` (not error)
- [ ] `find_base_schemas_by_ids` skips missing IDs, preserves order
- [ ] `delete_base_schema` is idempotent
- [ ] Error-path tests via `FailurePoint::BeforeRead` / `BeforeWrite` injection

## TDD Plan

Vertical-slice tracer-bullet approach (one test → one impl per cycle):

| Cycle | RED test (write first) | GREEN impl (minimal to pass) |
|-------|----------------------|------------------------------|
| 1 | `InMemoryRepository::save_base_schema` + `find_base_schema_by_id` roundtrip | Add `base_schemas` field, impl both methods |
| 2 | `find_base_schema_by_id` returns None for missing | Minimal None return |
| 3 | `find_base_schemas_by_ids` preserves order + skips missing | Batch iteration over HashMap |
| 4 | `delete_base_schema` removes + idempotent on missing | Remove from HashMap |
| 5 | RedbRepository `save` + `find` roundtrip | Table def + read/write impl |
| 6 | RedbRepository `find_base_schemas_by_ids` | Batch lookup |
| 7 | RedbRepository `delete_base_schema` | Simple remove |
| 8 | Error injection tests (FailurePoint coverage) | — |
| 9 | `save_many_base_schemas` (if easy) | Batch loop |

**Naming convention**: `find_*` prefix (not `get_*`), following `find_schema_by_id` pattern.
**Test structure**: `mod base_schema { mod lookup { ... } mod save { ... } mod delete { ... } }` per adapter.

## Implementation Log

### Commits

| Hash | Message |
|------|---------|
| `f9345a26` | docs(scratch): update issue 02 with review findings and TDD plan |
| `6268921a` | feat(schema): add BaseSchema repository contracts and storage |
| `d0776577` | refactor(schema): iterator pre-serialization, fix BASE_SCHEMA_BY_ID doc |

### Rust Best Practices Review (post-impl)

**Findings (3 minor, 0 critical):**

1. **Unnecessary clone** (`write.rs:245`): `save_many_base_schemas` used `bytes.as_slice().to_vec()` — `to_vec()` clones the vec unnecessarily when `bytes` is already `Vec<u8>`. Fixed: use `bytes` directly (via `bytes.to_vec()` — AlignedVec derefs to `[u8]`).

2. **Pre-serialization inside transaction**: Initially moved serialization inside the write transaction to match `save_many_schemas`. But the codebase policy is to serialize *outside* the transaction to keep tx time minimal. Reverted to pre-serialization with iterator chain: `.map().collect::<Result<Vec<_>, _>>()`.

3. **Stale doc for `BASE_SCHEMA_BY_ID`** (`tables.rs:97-105`): Omitted `PropertyMap` — BaseSchema also contains properties, not just extends/excludes. Fixed.

**Loops → iterators**: `save_many_base_schemas` pre-serialization converted from `for` loop to `.map().collect::<Result<Vec<_>, _>>()`. Inner insert loop stays as `for` — `try_for_each` doesn't match `Result<Option<AccessGuard>>` return from `table.insert`.

### Verification
- 1529 unit + integration tests pass
- `cargo clippy --all-targets` — 0 warnings
- `cargo fmt --check` — clean
- All pre-commit hooks passed on both commits (fmt, clippy, tests, conventional commits, gitleaks)
