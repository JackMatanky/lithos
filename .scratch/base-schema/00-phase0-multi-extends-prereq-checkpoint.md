---
title: 00-phase0-multi-extends-prereq-checkpoint
category: enhancement
label: ready-for-human
status: open
date_created: 2026-06-01
---

## Type

HITL

## Labels

- base-schema
- ready-for-human

## Parent

- `.scratch/base-schema/PRD.md`
- `.scratch/schema-processor-split/PRD.md`

## What to build

Confirm and close the cross-PRD prerequisite that Phase 0 (multi-parent extends alignment) is complete before BaseSchema Phase 1 starts.

This checkpoint is intentionally human-gated because it spans two PRDs and affects parsing, snapshots, and downstream semantics. It should produce a clear go/no-go decision for beginning the AFK BaseSchema slices.

## Acceptance criteria

- [x] `RawSchema.extends` has been migrated from `Option<SchemaName>` to `Vec<SchemaName>`.
- [x] `SchemaVersion.extends` has been migrated from `Option<SchemaName>` to `Vec<SchemaName>`.
- [x] Callers that read extends from raw/snapshot types compile and preserve current single-parent behavior.
- [x] A brief validation note is added to `.scratch/schema-processor-split/PRD.md` or linked issue comments indicating Phase 0 is complete.
- [x] BaseSchema issues `01` through `07` are explicitly unblocked.

## Blocked by

Phase 0 implementation and validation evidence.

## Agent Brief

> *This was generated by AI during triage.*

**Recommendation**: keep `ready-for-human` (cross-PRD prerequisite gate).

**Execution brief (HITL):**
- Validate Phase 0 changes are merged and visible in `RawSchema`/`SchemaVersion` extends shapes.
- Confirm downstream single-parent semantics still hold in current loader tests.
- Record explicit unblock note in `.scratch/schema-processor-split/PRD.md` (or linked issue notes) before releasing `01`-`07`.

**GitNexus context:**
- Index for `lithos` is stale (`19` commits behind); re-run analysis before final go/no-go.
- The schema load orchestration entrypoint is `lithos-core/src/schema/builder.rs:48` (`load_all`), with medium upstream blast radius and broad test coupling.

**Risk checks before unblocking:**
- Multiple-inheritance shape migration can ripple through compare/analyze paths and stale-reference behavior.
- Require at least one evidence run of schema loader + processor tests after confirming extends migration.

## Agent Brief (Updated)

> *This was generated by AI during triage.*

**Category:** enhancement
**Summary:** Keep this checkpoint blocked until Phase 0 multi-parent extends migration is actually complete in raw/snapshot types and validated end-to-end.

**Current behavior:**
- `RawSchema.extends` is still `Option<SchemaName>`.
- `SchemaVersion.extends` is still `Option<SchemaName>`.
- `schema_processor` still builds parent edges with single-parent assumptions (`Option` + `if let Some(parent)` flow).
- The issue currently says "can start immediately," but the concrete prerequisite acceptance criteria are not yet satisfied.

**Desired behavior:**
- Phase 0 migration is complete and merged such that both raw and snapshot extends fields are list-based and preserve single-parent behavior.
- Existing loader/processor behavior remains semantically stable for no-parent and single-parent schemas.
- Evidence of completion is recorded in the parent schema-processor split planning artifacts before unblocking BaseSchema slices `01`-`07`.

**Key interfaces:**
- `RawSchema` (`extends` field + accessor contract) — migrate from optional single parent to list shape.
- `SchemaVersion` (`extends` field + accessor/constructor contract) — migrate from optional single parent to list shape.
- Schema graph construction flow (`schema_processor` parent-edge construction) — must consume list-based extends deterministically.
- Phase-0/BaseSchema planning contract — unblocking decision must reference concrete validation evidence, not assumption.

**Acceptance criteria:**
- [ ] `RawSchema.extends` uses a list shape and no longer uses `Option<SchemaName>`.
- [ ] `SchemaVersion.extends` uses a list shape and no longer uses `Option<SchemaName>`.
- [ ] Parent-edge construction in schema processing consumes list-based extends (with unchanged behavior for empty/single parent cases).
- [ ] Targeted schema loader and schema processor tests pass with evidence attached to this checkpoint.
- [ ] `.scratch/schema-processor-split/PRD.md` (or linked notes) explicitly records Phase 0 completion and unblocks BaseSchema `01`-`07`.

**Out of scope:**
- Implementing BaseSchema Phase 1 slices in this checkpoint.
- Rewriting inheritance semantics beyond enabling list-based extends compatibility in Phase 0.

## Approved Clarifications

- Duplicate parent names in extends input should be accepted in this phase and normalized/deduped in later processing.
- Parent order has no semantic meaning; keep this explicit for follow-up design decisions where `Vec<T>` vs `Box<[T]>` vs set-like behavior is evaluated.
- No explicit migration notes are required for this checkpoint.

## Approved TDD Plan

Use vertical RED-GREEN cycles (one behavior per test) and keep tests behavior-focused through public interfaces.

1. Raw schema shape migration
   - Add/adjust tests for `RawSchema` parse/accessor behavior with empty/single/multi extends forms.
2. Snapshot conversion migration
   - Add/adjust tests for `SchemaVersion::new` and accessors to ensure extends list is preserved.
3. Graph construction behavior
   - Add/adjust tests for schema graph parent-edge construction consuming list-based extends.
4. Compatibility behavior
   - Verify no-parent and single-parent semantics remain unchanged in loader/processor outcomes.
5. Regression and evidence run
   - Run targeted schema processor + schema loader tests and capture evidence for prerequisite closeout.

Test naming and module organization must follow:
- `docs/engineering/testing/unit.md`
- `docs/engineering/testing/unit-naming.md`

## Validation Evidence

- `cargo test -p lithos-core raw::aggregate::tests::deserialization` (pass)
- `cargo test -p lithos-core schema::views::snapshots::tests::schema_version` (pass)
- `cargo test -p lithos-core schema::schema_processor::tests::build_new_graph_adds_all_resolvable_parents_from_extends_list` (pass)
- `cargo test -p lithos-core --test schema_loader` (pass)

## Unblock Decision

Phase 0 prerequisite is complete and validated. BaseSchema issues `01` through `07` are unblocked for agent execution.
