---
title: 05-add-filesystem-tracker
category: enhancement
label: ready-for-agent
status: ready
branch: issue-05-add-filesystem-tracker
merge_commit: null
date_created: 2026-06-29
date_completed: null
---

# Add Filesystem Tracker

## Parent

.scratch/settings-redesign/PRD.md

## What to build

Add the minimal filesystem tracking layer for consumed config files. Tracking is path-hash symlinks under the configured tracking directory and is used for diagnostics/cleanup, not for config snapshot persistence.

This slice adds the replacement mechanism before the old repository storage is removed.

## Acceptance criteria

- [ ] Internal `Tracker` provides `track`, `list_all`, and `clean` operations.
- [ ] Tracking uses a stable hash of canonicalized config file paths.
- [ ] Tracking creates symlinks from `TRACKED_CONFIGS/<path-hash>` to the canonical file path.
- [ ] Tracking is idempotent when called repeatedly for the same path.
- [ ] `clean` removes dangling tracking symlinks.
- [ ] Tempdir tests cover track, list, idempotency, and clean.

## Blocked by

- .scratch/settings-redesign/02-make-discovery-internal-and-linear.md

## Triage Notes

> *This was generated by AI during triage.*

**Verdict**: `enhancement` + `ready-for-agent`.

This replacement mechanism is clear and small. It should land before persistence removal, but it does not need to solve trust UX or config persistence.

### Agent Brief

- Implement only the path-hash symlink tracker: track, list, clean.
- Keep the module internal to settings.
- Use canonicalized paths for hashing and symlink targets where possible; return typed IO/path errors when canonicalization fails.
- Do not store config contents, versions, raw views, or merge snapshots.
- Keep tracking directory creation lazy and localized to tracker operations.

### GitNexus Context

- Old settings persistence is currently repository/redb-backed. This issue adds the replacement primitive but must not remove repository consumers.

### Rust Best-Practices Notes

- Use std filesystem/symlink APIs; do not add dependencies for this.
- Keep hash/path helpers private unless tests require public behavior through module API.
- Tempdir tests should cover idempotent track, list, dangling clean, and missing target errors.
