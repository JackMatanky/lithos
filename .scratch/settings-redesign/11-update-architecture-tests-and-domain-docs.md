---
title: 11-update-architecture-tests-and-domain-docs
category: enhancement
label: ready-for-agent
status: ready
branch: issue-11-update-architecture-tests-and-domain-docs
merge_commit: null
date_created: 2026-06-29
date_completed: null
---

# Update Architecture Tests And Domain Docs

## Parent

.scratch/settings-redesign/PRD.md

## What to build

Bring architecture tests and domain documentation in line with the completed settings pipeline redesign. This slice should document the final state after migration and deletion, not paper over temporary compatibility shims from earlier slices.

## Acceptance criteria

- [ ] Settings context docs use the final domain language: SettingsService, AppConfig, GlobalConfig, LocalConfig, RawConfig, CandidatePath, DiscoveryOutcome, Tracker, Trust.
- [ ] Stale terms from the removed persistence model are deleted or explicitly marked historical where needed.
- [ ] Architecture tests assert discovery is internal and settings has one inbound service boundary.
- [ ] Architecture tests assert the final crate module layout: `CandidatePath` at `src/candidate.rs`, `Tracker` and `Trust` under `config/`, `service.rs`/`location.rs`/`env.rs`/`os_dirs.rs` flat at `src/`.
- [ ] Architecture tests assert settings no longer exposes repository/storage/snapshot/version APIs.
- [ ] ADR supersession notes from the PRD are reflected in the relevant docs.
- [ ] Documentation matches public exports and tested behavior.

## Blocked by

- .scratch/settings-redesign/07-remove-settings-persistence-model.md
- .scratch/settings-redesign/09-rewire-existing-cli-commands.md
- .scratch/settings-redesign/10-add-trust-cli-commands.md

## Triage Notes

> *This was generated by AI during triage.*

**Verdict**: `enhancement` + `ready-for-agent`.

This is correctly last. It should document and enforce the final architecture after migration/removal, not transitional compatibility.

### Agent Brief

- Update settings context docs to final language only.
- Update architecture tests so they assert the new service boundary, internal discovery, removed persistence model, and cleaned exports.
- Remove stale terms unless explicitly documenting superseded history.
- Reflect ADR supersession notes from the PRD.
- Verify docs match actual public exports after all code slices land.

### GitNexus Context

- Current architecture tests still encode old discovery/builder assumptions.
- GitNexus context itself was stale during triage; final implementer should use direct source and optionally re-index before adjusting architecture assertions.

### Rust Best-Practices Notes

- Public APIs introduced by earlier slices should have rustdoc with error behavior.
- Avoid comments that restate code; move lasting rationale to context docs or ADR notes.
- Architecture tests should be narrow and durable, checking boundaries rather than exact private module layout where possible.
