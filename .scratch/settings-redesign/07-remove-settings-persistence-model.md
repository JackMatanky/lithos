---
title: 07-remove-settings-persistence-model
category: enhancement
label: ready-for-agent
status: ready
branch: issue-07-remove-settings-persistence-model
merge_commit: null
date_created: 2026-06-29
date_completed: null
---

# Remove Settings Persistence Model

## Parent

.scratch/settings-redesign/PRD.md

## What to build

Delete the old settings persistence model only after all production callers have migrated to the new discovery, config builder, tracker, and trust paths. This is a cleanup/removal slice, not a migration slice.

If any component still imports the old repository, storage, version, view, event, or processor APIs, migrate that caller first instead of deleting underneath it.

## Acceptance criteria

- [ ] No production caller imports settings repository/storage APIs before deletion.
- [ ] Old config repository traits and storage modules are removed from settings.
- [ ] Old config snapshot views, version counters, events, stale-analysis processor, and resolver plan are removed or made unreachable only in tests if temporarily necessary.
- [ ] Settings crate no longer depends on `traces-db`, `redb`, or `rkyv` for config persistence.
- [ ] Public exports no longer expose old `Config`, `Vault`, `Global`, repository, storage, or discovery-port names.
- [ ] Remaining crate structure matches the PRD layout: `candidate.rs` at `src/`, tracker/trust under `config/`, `location.rs`/`env.rs`/`os_dirs.rs` flat at `src/`.
- [ ] Compile errors from removed components are fixed by using the new SettingsService and AppConfig path, not by adding compatibility wrappers.
- [ ] Architecture tests assert the persistence model is gone.

## Out of Scope

- Adding any new functionality (this slice is deletion-only)
- Migrating callers — if a production caller still imports an old component, fix that caller first, then delete
- Updating domain docs or architecture tests (issue 11)
- CLI command rewiring (issue 09)
- Dependency removal confirmation: after deletion, `traces-db`, `redb`, and `rkyv` should be removable from settings `Cargo.toml` without breaking compilation

## Blocked by

- .scratch/settings-redesign/04-build-ephemeral-config-pipeline.md
- .scratch/settings-redesign/05-add-filesystem-tracker.md
- .scratch/settings-redesign/06-add-trust-and-ignore-system.md

## Triage Notes

> *This was generated by AI during triage.*

**Verdict**: `enhancement` + `ready-for-agent`, with strict sequencing.

This issue is ready only as a cleanup ticket. The maintainer explicitly clarified that components must be migrated away from before removal; this issue now treats deletion as the last step after all production imports are gone.

### Agent Brief

- Start with a repo-wide import/use search for old settings persistence APIs.
- If any production caller still needs an old component, migrate that caller first or stop and report the blocker.
- Delete repository/storage/views/events/versions only after new builder/tracker/trust paths are active.
- Remove settings dependencies on `traces-db`, `redb`, and persistence-only `rkyv` usage only after compile proves they are unused.
- Update exports by removing compatibility wrappers, not by renaming old types to preserve behavior.

### GitNexus Context

- GitNexus currently sees old `Builder` repository fields and app/CLI bootstrap calls, so direct source search is mandatory before deletion.
- The current indexed flow confirms CLI command handlers still reach old bootstrap/build APIs in stale graph data.

### Rust Best-Practices Notes

- Deletion should reduce error hierarchies; do not leave dead repository error variants for future speculation.
- Prefer compile failures as migration signals, then fix by moving callers to SettingsService/AppConfig.
- Architecture tests should prove removed APIs are not exported.
