---
title: 06-add-trust-and-ignore-system
category: enhancement
label: ready-for-agent
status: ready
branch: issue-06-add-trust-and-ignore-system
merge_commit: null
date_created: 2026-06-29
date_completed: null
---

# Add Trust And Ignore System

## Parent

.scratch/settings-redesign/PRD.md

## What to build

Add the trust and ignore system used by SettingsService and future CLI trust commands. Trust state is stored as path-hash symlinks, ignored configs are filtered or skipped, global configs are trusted automatically, and paranoid/CI behavior is represented in the trust mode.

This slice creates the new trust path before old security/persistence assumptions are removed.

## Acceptance criteria

- [ ] `Trust` lives in `config/trust.rs` and is `pub` (re-exported at crate root for CLI commands). Called internally by `ConfigBuilder` during the trust phase.
- [ ] Public `Trust` API supports trust, untrust, ignore, trusted check, ignored check, and status/listing needed by CLI.
- [ ] Trust and ignore entries use stable path-hash symlinks.
- [ ] Global config candidates are treated as trusted.
- [ ] CI mode trusts without prompting.
- [ ] Paranoid mode verifies trusted content hashes according to the PRD’s security model.
- [ ] Safe configs without trust-requiring directives can skip prompts.
- [ ] ConfigBuilder’s trust phase uses the trust system and skips ignored candidates.
- [ ] Trust directory lives at `TRUSTED_CONFIGS` path constant from `src/location.rs`.
- [ ] Ignore directory lives at `IGNORED_CONFIGS` path constant from `src/location.rs`.
- [ ] Tempdir tests cover trust, untrust, ignore, status, CI mode, and paranoid mismatch.

## Out of Scope

- CLI trust command routing or clap subcommands (issue 10)
- Interactive prompt UI — Trust exposes `trust_check()` but prompting is handled by the caller
- Old security/persistence model removal (issue 07)
- ConfigBuilder integration — trust phase calls Trust internally, but this slice only provides the module

## Blocked by

- .scratch/settings-redesign/02-make-discovery-internal-and-linear.md
- .scratch/settings-redesign/05-add-filesystem-tracker.md

## Triage Notes

> *This was generated by AI during triage.*

**Verdict**: `enhancement` + `ready-for-agent`.

This is ready as an AFK slice because the PRD already fixes the trust model. Keep it storage-focused and deterministic; interactive CLI behavior is covered later.

### Agent Brief

- Implement Trust over symlink state and expose only the API needed by SettingsService and CLI.
- Integrate trust checks into ConfigBuilder once builder and tracker are available.
- Treat global configs as trusted and CI mode as non-interactive trust.
- Implement ignore so ignored candidates are skipped before loading.
- Do not let CLI code own trust storage rules.

### GitNexus Context

- CLI trust commands do not exist yet; this issue should avoid creating command routing.
- Current config/doctor/index flows still depend on old bootstrap, so trust integration should be through new builder/service paths only.

### Rust Best-Practices Notes

- Model trust-mode behavior with small enums and typed errors.
- Guard any interactive prompt with a minimal global lock only if needed; document the ceiling if used.
- Test trust, untrust, ignore, global auto-trust, CI mode, and paranoid mismatch separately.
