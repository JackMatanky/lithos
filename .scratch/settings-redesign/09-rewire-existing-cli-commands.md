---
title: 09-rewire-existing-cli-commands
category: enhancement
label: ready-for-agent
status: ready
branch: issue-09-rewire-existing-cli-commands
merge_commit: null
date_created: 2026-06-29
date_completed: null
---

# Rewire Existing CLI Config Doctor Index Commands

## Parent

.scratch/settings-redesign/PRD.md

## What to build

Migrate existing CLI commands to the new BootstrapRunner/SettingsService path while preserving command behavior. `config`, `config files`, `doctor`, and `index` should no longer depend on discovery ports, old discovery flags, or in-memory settings repositories.

This slice removes CLI usage of old components before the components are deleted.

## Acceptance criteria

- [ ] `traces config` uses BootstrapRunner/SettingsService and preserves human and JSON output semantics.
- [ ] `traces config files` uses discovery-only SettingsService flow and still exits successfully when discovery fails.
- [ ] `traces doctor` uses the new full config and discovery-only flows.
- [ ] `traces index` obtains AppConfig and cache setup through BootstrapRunner without pre-creating settings repository state.
- [ ] CLI tests are updated away from `DiscoveryService`, `DiscoveryFlags`, and `InMemoryRepository`.
- [ ] User-facing diagnostics remain actionable and avoid leaking internal pipeline names unless already part of CLI contract.

## Blocked by

- .scratch/settings-redesign/08-rename-bootstrapper-to-bootstraprunner.md

## Triage Notes

> *This was generated by AI during triage.*

**Verdict**: `enhancement` + `ready-for-agent`.

This is the consumer-migration slice that must happen before old settings components are removed. It is ready after BootstrapRunner exists.

### Agent Brief

- Migrate `config`, `config files`, `doctor`, and `index` handlers to BootstrapRunner/SettingsService.
- Preserve output formats and failure semantics, especially `config files` always returning success.
- Remove handler/test dependencies on `DiscoveryService`, `DiscoveryFlags`, and `InMemoryRepository`.
- Do not introduce trust command routing in this issue.
- Keep CLI as orchestration only; domain rules stay in settings/app.

### GitNexus Context

- `run_config` is called by CLI `run_main` and tests; it calls old `Bootstrapper.run` and `run_discovery_only`.
- `run_index` uses discovery-only bootstrap and builds index command state from discovered paths.
- These are direct execution-flow edges, so this slice is the key migration gate before deletion.

### Rust Best-Practices Notes

- Keep command handlers small and pass borrowed paths/output writers.
- Tests should assert observable CLI output and exit behavior, not internal service implementation.
- Avoid `unwrap`/`expect` in production output code; test helpers may use them.
