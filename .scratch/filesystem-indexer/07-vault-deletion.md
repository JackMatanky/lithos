---
title: 07-indexer-vault-deletion
category: enhancement
label: ready-for-human
status: open
branch:
merge_commit:
date_created: 2026-06-09
date_completed:
---

# Issue 07: Delete `lithos-core::vault`

## What to build

Remove `lithos-core::vault` once the Indexer storage adapter has passed full
integration tests (issue 03 complete) and the Indexer application service is
proven (issue 04 complete). The Vault module is prior art and migration source;
it is not the long-term API. Keeping it any longer than necessary risks
confusion about which module owns filesystem node state.

The agent should:

1. Audit every import of `lithos-core::vault` across the workspace and
   replace each with the equivalent Indexer type or repository call.
2. Remove the `vault` module declaration from `lithos-core/src/lib.rs`.
3. Delete `lithos-core/src/vault/`.
4. Confirm all tests pass and no dead-code warnings remain.
5. Open the PR for human review before merging — this is a destructive
   migration and warrants explicit sign-off.

## Acceptance criteria

- [ ] `lithos-core::vault` module and all its files are deleted.
- [ ] No remaining imports of `lithos_core::vault` anywhere in the workspace.
- [ ] `IndexerError` variants are reviewed and confirmed to cover all error
      conditions previously handled by the deleted Vault error types.
- [ ] All 1588+ tests pass (`mise run test`).
- [ ] No clippy warnings (`mise run lint`).
- [ ] PR description explains what each replaced call site was doing and what
      it was replaced with.
- [ ] Human reviewer has approved the PR before merge.

## Blocked by

- 03-ports-and-adapters.md
- 04-application-service.md
- Refactor `crates/note/` integration tests to remove `traces_vault::VaultProcessor` dependency

---

## Triage Notes (v2 — corrected)

> *This was generated by AI during triage.*

**Updated Verdict**: `ready-for-human` — label stands, but **issue is NOT ready for execution**. Previous triage missed critical dependencies introduced by the workspace restructure.

**What the original triage missed:**

The initial analysis was based on the old `lithos-core/src/vault/` flat layout. After the workspace was restructured to `crates/`:

- **`crates/note/Cargo.toml:44`** declares `traces-vault = { path = "../vault" }` as a dev-dependency.
- **`crates/note/tests/note_reader.rs:43`** imports `traces_vault::VaultProcessor` and uses it as the primary fixture mechanism (`build_fixture()`, `build_environment()`) for ALL integration tests (7 test functions).
- **`crates/vault/src/processor.rs`** imports from `traces_note` — bidirectional coupling exists (`processor.rs:20-25`).

GitNexus impact analysis on `VaultProcessor` (struct at `crates/vault/src/processor.rs:37`) confirms:

- **Risk: HIGH** — 2 direct callers, 3 affected modules, 11 impacted symbols total.
- Will break all 7 integration tests in `crates/note/tests/note_reader.rs`.
- Will break doc comments in `crates/db/src/table.rs`.

**Why it's not ready:**

The `note` integration tests depend on `VaultProcessor::process_full()` to bootstrap their test fixtures. Before vault can be deleted, the note crate needs its own self-contained test harness (directly instantiating note storage + processor without vault). This requires a refactoring pass on `crates/note/` that is not currently scoped in any issue.

**Recommended path forward:**

1. Create a new issue: "Refactor note integration tests to remove vault dependency."
2. Add it as a blocker to this issue.
3. Re-triage once that refactoring is complete and there are zero remaining imports of `traces_vault` outside the vault crate itself.
