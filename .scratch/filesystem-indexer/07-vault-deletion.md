---
title: 07-indexer-vault-deletion
category: enhancement
label: ready-for-human
status: open
branch:
merge_commit:
date_created: 2026-06-09
date_completed:
---

# Issue 07: Delete `lithos-core::vault`

## What to build

Remove `lithos-core::vault` once the Indexer storage adapter has passed full
integration tests (issue 03 complete) and the Indexer application service is
proven (issue 04 complete). The Vault module is prior art and migration source;
it is not the long-term API. Keeping it any longer than necessary risks
confusion about which module owns filesystem node state.

The agent should:

1. Audit every import of `lithos-core::vault` across the workspace and
   replace each with the equivalent Indexer type or repository call.
2. Remove the `vault` module declaration from `lithos-core/src/lib.rs`.
3. Delete `lithos-core/src/vault/`.
4. Confirm all tests pass and no dead-code warnings remain.
5. Open the PR for human review before merging — this is a destructive
   migration and warrants explicit sign-off.

## Acceptance criteria

- [ ] `lithos-core::vault` module and all its files are deleted.
- [ ] No remaining imports of `lithos_core::vault` anywhere in the workspace.
- [ ] All 1588+ tests pass (`mise run test`).
- [ ] No clippy warnings (`mise run lint`).
- [ ] PR description explains what each replaced call site was doing and what
      it was replaced with.
- [ ] Human reviewer has approved the PR before merge.

## Blocked by

- 03-ports-and-adapters.md
- 04-application-service.md

---

## Triage Notes

> *This was generated by AI during triage.*

**Verdict**: `ready-for-human` — confirmed. Label is correct.

**What was checked:**

- PRD Section 15 (Migration Notes) explicitly states: "Vault is deleted in a dedicated follow-on PR. This keeps Indexer issues focused and makes the deletion reviewable in isolation."
- PRD (handoff key decision 6) states: "agent-led but HITL — agent does the work, human reviews the PR before merge." The `ready-for-human` label reflects this HITL intent correctly.
- PR-review AC is explicit: "PR description explains what each replaced call site was doing and what it was replaced with" + "Human reviewer has approved the PR before merge." Both are present and unambiguous.
- Blocker chain (#03 + #04) is correct. Issues #05 and #06 are intentionally not blockers — the Vault can be deleted once the Indexer storage is proven, even before CLI wiring. Correct per PRD.
- GitNexus impact analysis on `lithos-core/src/vault` folder: **LOW risk**. Zero external imports of `lithos_core::vault` from outside the vault subdirectory. The only cross-boundary reference is a doc comment in `vault/storage/mod.rs`. The `pub mod vault` declaration in `lithos-core/src/lib.rs` is the only lib-level entry point, and the graph shows no downstream consumers outside vault itself.
- 1588+ test count as AC lower bound — correct (PRD confirmed "1588 tests pass after deletion of `lithos-core::application`").

**No gaps found. Label is appropriate.**
