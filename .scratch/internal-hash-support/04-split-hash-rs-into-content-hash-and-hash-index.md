---
title: 04-split-hash-rs-into-content-hash-and-hash-index
category: enhancement
label: ready-for-agent
status: completed
date_created: 2026-05-31T00:00:00+00:00
date_completed: 2026-05-31T00:00:00+00:00
---

# Split `hash.rs` into `content_hash.rs` and `hash_index.rs`

## What to build

Mechanically split `lithos-core/src/support/hash.rs` into two files with no behavioral change:

- **`content_hash.rs`**: `Blake3Hash`, `ArchivedBlake3Hash`, `HashInput` enum, all `From` impls for `HashInput`, `hash_bytes()`, `hash_structured()` helpers, and their tests. Module doc updated to say "content hashing primitives" instead of "centralised hashing utilities."
- **`hash_index.rs`**: `Blake3HashIndex`, `ArchivedBlake3HashIndex`, diff helpers (`changed_keys`, `removed_keys`), `From`/`Default` impls, and their tests. Module doc added describing the keyed hash map for change detection.

Update `support/mod.rs` facade to `pub(crate) mod content_hash; pub(crate) mod hash_index;` and adjust the re-exports block accordingly.

Update all import paths across the workspace that currently reference `support::hash::*` to the correct new submodule (`support::content_hash::*` or `support::hash_index::*`).

## Acceptance criteria

- [x] `hash.rs` removed; `content_hash.rs` and `hash_index.rs` exist under `support/`.
- [x] `support/mod.rs` facade re-exports the same symbols with updated paths; no public API change.
- [x] All callers updated: `support::hash::Blake3Hash` → `support::content_hash::Blake3Hash`, `support::hash::Blake3HashIndex` → `support::hash_index::Blake3HashIndex`, etc.
- [x] `cargo clippy -p lithos-core --all-targets -- -D warnings` passes.
- [x] `cargo test -p lithos-core` passes with no regressions.

## Blocked by

None — can start immediately.

---

## Triage Assessment

> *This was generated by AI during triage.*

### Completeness

The issue is well-specified with clear scope, concrete file contents, and testable AC. Two minor gaps found:

1. **Doc comments in `schema/views/hashes.rs`** (lines 144, 167, 191, 212, 234, 258) use `crate::support::hash::Blake3Hash` — they need updating to `crate::support::content_hash::Blake3Hash`. The AC's "etc." covers this loosely but should be explicit.

2. **`ArchivedBlake3Hash` / `ArchivedBlake3HashIndex`** are not listed in the "What to build" section but are correctly implied by the type lists. Explicitly mention them for clarity.

### Consistency

Internally consistent. The content-hash vs hash-index boundary is clean:
- Content hashing: Blake3Hash (newtype), HashInput (enum + From impls), hash_bytes, hash_structured, ArchivedBlake3Hash
- Hash indexing: Blake3HashIndex (generic wrapper with key diff helpers), ArchivedBlake3HashIndex

### Impact — LOW risk

- **11 files** import from `support::hash::*` or `crate::support::hash::*`
- **1 file** (`vault/model.rs`) uses the indirect re-export path `crate::support::Blake3Hash` — no change needed; the facade preserves it
- All types are `pub(crate)` — no downstream external breakage
- All changes are compile-time detectable; compiler catches missed imports

### Affected files (complete list)

| File | What changes | Risk |
|------|-------------|------|
| `support/hash.rs` | **Deleted** — replaced by two files | — |
| `support/content_hash.rs` | **New** — content hash types + helpers + tests | — |
| `support/hash_index.rs` | **New** — hash index types + diff helpers + tests | — |
| `support/mod.rs` | Facade: `mod hash` → `mod content_hash; mod hash_index;`, re-exports adjusted | Low |
| `schema/schema_processor.rs` | Import: `support::hash::Blake3Hash` → `support::content_hash::Blake3Hash` | Low |
| `schema/property_bank_processor.rs` | Same | Low |
| `schema/views/snapshots.rs` | Same | Low |
| `schema/views/raw.rs` | Same | Low |
| `schema/views/contracts.rs` | Same | Low |
| `schema/views/hashes.rs` | Import: split `support::hash::{Blake3Hash, Blake3HashIndex}` → `content_hash::Blake3Hash` + `hash_index::Blake3HashIndex`; 6 doc example paths | Low |
| `schema/raw/property.rs` | Import: `Blake3Hash, Blake3HashIndex, hash_structured` → `content_hash::Blake3Hash, hash_structured` + `hash_index::Blake3HashIndex` | Low |
| `schema/delta.rs` | Import: `Blake3Hash, hash_structured` → `content_hash::Blake3Hash, hash_structured` | Low |
| `config/views.rs` | Import: `support::hash::Blake3Hash` → `support::content_hash::Blake3Hash` | Low |
| `config/processor.rs` | 7 references: inline `use crate::support::hash::Blake3Hash` (×2), type paths `crate::support::hash::Blake3HashIndex<...>` (×2), `crate::support::hash::Blake3Hash` (×3) | Low |
| `vault/model.rs` | Uses `crate::support::Blake3Hash` via re-export — **no change needed** | None |

### Tests affected

- Tests in `hash.rs` (`mod constructor`, `mod builders`, `mod index`, `mod validation`, `mod borrowing`, `mod conversions`) — move with their types, zero assertion changes
- All callers' existing tests — zero assertion changes

### Hidden gotchas

- **`#[expect(dead_code, ...)]` annotations** on `Blake3HashIndex` impl block (line 116) and diff helpers (line 221) must be preserved in `hash_index.rs`
- **`hash_bytes()`** is `#[cfg(test)]` — ensure it remains test-gated in `content_hash.rs`
- **`serde::Serialize` bound** on `hash_structured()` — `content_hash.rs` must import or reference `serde`
- **Doc comments** in `hashes.rs` (6 references) — easy to miss; treat as first-class callers

## Agent Brief

**Category:** enhancement
**Summary:** Mechanically split `lithos-core/src/support/hash.rs` into `content_hash.rs` and `hash_index.rs`

**Current behavior:**
All content hashing primitives (`Blake3Hash`, `HashInput`, `hash_structured`) and hash indexing (`Blake3HashIndex`, diff helpers) live in a single 536-line file `support/hash.rs`. The `support/mod.rs` facade re-exports `Blake3Hash, Blake3HashIndex, HashInput, hash_structured`.

**Desired behavior:**
The file is split into two single-responsibility files with no behavioral change. All import paths are updated across the workspace. `cargo clippy -D warnings` and `cargo test -p lithos-core` pass unchanged.

**Key interfaces:**
- `Blake3Hash`, `ArchivedBlake3Hash`, `HashInput`, `hash_bytes()`, `hash_structured()` — move to `content_hash.rs`
- `Blake3HashIndex<K>`, `ArchivedBlake3HashIndex<K>`, `changed_keys()`, `removed_keys()` — move to `hash_index.rs`
- `support::hash` → replaced by `support::content_hash` and `support::hash_index`
- `support::Blake3Hash` (re-export path) — unchanged; facade preserved

**Acceptance criteria:**
- [ ] `hash.rs` removed; `content_hash.rs` and `hash_index.rs` exist under `support/`
- [ ] `support/mod.rs` re-exports the same symbols from the new submodules; no public API change
- [ ] All `support::hash::*` and `crate::support::hash::*` import paths updated to new submodules (11 files + doc comments)
- [ ] `cargo clippy -p lithos-core --all-targets -- -D warnings` passes
- [ ] `cargo test -p lithos-core` passes with no regressions
- [ ] Doc examples in `schema/views/hashes.rs` (6 references) updated to `crate::support::content_hash::Blake3Hash`
- [ ] `#[expect(dead_code, ...)]` annotations preserved on their respective types in `hash_index.rs`

**Implementation sequence:**
1. Create `content_hash.rs` with content hash types, `HashInput` + `From` impls, `hash_bytes()`/`hash_structured()`, and their tests
2. Create `hash_index.rs` with `Blake3HashIndex`, `ArchivedBlake3HashIndex`, diff helpers, `From`/`Default`, and its tests
3. Update `support/mod.rs` facade (mod declarations + re-exports)
4. Remove `hash.rs`
5. Update all import paths across 11 files + 6 doc comments in `hashes.rs`
6. `cargo clippy -D warnings` and `cargo test -p lithos-core` to verify
7. `cargo fmt` to finalize

**Out of scope:**
- Any behavioral change to the types or functions
- Adding new types, traits, or public API surface
- Changing visibility of existing types (all remain `pub(crate)`)
- Refactoring the hash algorithm or data structures
- Adding/removing rkyv derives or other derives on existing types
