---
title: 03-rename-and-adapt-view-hash-index-wrapper
category: enhancement
label: ready-for-human
status: completed
date_created: 2026-05-08T13:21:23+03:00
date_completed: 2026-05-08T13:47:11+03:00
---

# Rename and adapt view hash index wrapper

## Blocked by

- `.scratch/hash-index-refactor/issue-002-raw-property-map-emits-hash-index.md`

## Related

- `.scratch/hash-index-refactor/issue-001-hash-index-visibility-propagation.md`
- `.scratch/visibility-hardening/issue-001-crate-visibility-policy-and-rollout.md`

## What to build

Rename `RawPropertyMapHash` to `RawPropertyHashIndex` in the schema views context, and keep it as the view-seam adapter over `Blake3HashIndex<PropertyName>`. Update all call sites and docs to use the new domain term while preserving behavior.

## Acceptance criteria

- [x] Type renamed from `RawPropertyMapHash` to `RawPropertyHashIndex` across schema views and dependents.
- [x] Wrapper remains a thin adapter over `Blake3HashIndex<PropertyName>` with minimal duplicated map mechanics.
- [x] `cargo clippy -p lithos-core --all-targets -- -D warnings` passes.
- [x] Targeted schema view/delta/raw property tests pass.

## Implementation notes

- Implemented in commit `63f1cda5` (`refactor(schema): split raw hash index from view wrapper`).
- View wrapper type was renamed from `RawPropertyMapHash` to `RawPropertyHashIndex`.
- Wrapper remains a thin adapter over `Blake3HashIndex<PropertyName>` and call paths now convert via `.into()` where needed.
- Schema view modules and dependent processors/storage paths were updated to the new type name without behavior drift.
- Pre-commit quality gates for the commit passed, including formatting, clippy, and tests.

## Changed files

- `lithos-core/src/schema/views/hashes.rs`
- `lithos-core/src/schema/views/mod.rs`
- `lithos-core/src/schema/views/properties.rs`
- `lithos-core/src/schema/views/raw.rs`
- `lithos-core/src/schema/views/snapshots.rs`
- `lithos-core/src/schema/storage.rs`
- `lithos-core/src/config/processor.rs`

## Follow-up validation

- [ ] Run `mise run test:unit:schema` to verify renamed wrapper coverage and unchanged view/delta behavior.
- [ ] Run `mise run lint` to ensure no stale type-name references remain after incremental edits.
- [ ] Run `mise run verify` before merge to reconfirm full workspace quality gates.

## Triage Notes

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Rename the schema view hash wrapper and keep it as a thin adapter over the crate-level hash index.

**Current behavior:**
The view-layer wrapper is named `RawPropertyMapHash` and is adapting toward `Blake3HashIndex<PropertyName>`; naming now conflicts with intended architecture where raw ingestion emits the index and view seams provide view-specific adapters.

**Desired behavior:**
Rename `RawPropertyMapHash` to `RawPropertyHashIndex` in schema views and dependent code. Keep it as a minimal adapter over `Blake3HashIndex<PropertyName>` with no unnecessary duplicated map mechanics.

**Key interfaces:**
- View hash metadata type used by `HashRecord` and view snapshot contracts.
- Conversion seam from raw ingestion index output into the view wrapper.
- Callers/tests/docs that currently reference `RawPropertyMapHash`.

**Acceptance criteria:**
- [ ] Type renamed from `RawPropertyMapHash` to `RawPropertyHashIndex` across schema views and dependents.
- [ ] Wrapper remains a thin adapter over `Blake3HashIndex<PropertyName>`.
- [ ] `cargo clippy -p lithos-core --all-targets -- -D warnings` passes.
- [ ] Targeted schema view/delta/raw property tests pass.

**Out of scope:**
- Changing `Blake3HashIndex` visibility (`pub(crate)` must remain).
- Broader hash strategy changes outside schema property hash views.
