---
title: 02-raw-property-map-emits-hash-index
category: enhancement
label: ready-for-human
status: completed
date_created:
date_completed:
---

# Raw Property Map emits hash index

## Related

- `.scratch/hash-index-refactor/issue-001-hash-index-visibility-propagation.md`
- `.scratch/visibility-hardening/issue-001-crate-visibility-policy-and-rollout.md`

## What to build

Refactor schema raw ingestion so `RawPropertyMap::compute_hashes()` produces `Blake3HashIndex<PropertyName>` directly, instead of a view-defined wrapper type. Keep this fully crate-internal and preserve existing incremental behavior.

## Acceptance criteria

- [x] `RawPropertyMap::compute_hashes()` returns `Blake3HashIndex<PropertyName>`.
- [x] No public API exposes `Blake3HashIndex` (it remains `pub(crate)`).
- [x] Existing schema delta/property hashing behavior remains unchanged, verified by targeted tests.

## Implementation notes

- Implemented in commit `63f1cda5` (`refactor(schema): split raw hash index from view wrapper`).
- `RawPropertyMap::compute_hashes()` now returns `Blake3HashIndex<PropertyName>` from the raw seam.
- Visibility seam was aligned so crate-private type visibility is not leaked through wider function exposure.
- Downstream schema paths were updated to consume the crate-level index output without changing hashing semantics.
- Pre-commit quality gates for the commit passed, including formatting, clippy, and tests.

## Changed files

- `lithos-core/src/schema/raw/property.rs`
- `lithos-core/src/schema/delta.rs`
- `lithos-core/src/schema/property_bank_processor.rs`
- `lithos-core/src/schema/schema_processor.rs`

## Follow-up validation

- [ ] Run `mise run test:unit:schema` to reconfirm schema hashing and delta behavior in isolation.
- [ ] Run `mise run verify` before merge to confirm full workspace gates still pass on latest `main`.
- [ ] Spot-check that no public API docs or signatures expose `Blake3HashIndex` outside crate boundaries.

## Blocked by

None - can start immediately.

## Triage Notes

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Make raw schema property hashing emit the crate-level hash index type directly.

**Current behavior:**
`RawPropertyMap::compute_hashes()` returns a view-defined wrapper type, which couples raw ingestion output semantics to view naming.

**Desired behavior:**
`RawPropertyMap::compute_hashes()` should return `Blake3HashIndex<PropertyName>` directly. The function remains crate-internal behavior-wise and must preserve existing hash/delta semantics.

**Key interfaces:**
- `RawPropertyMap::compute_hashes()` return type: switch to `Blake3HashIndex<PropertyName>`.
- Hash-comparison callers in schema delta and related tests: consume index output without behavior drift.
- Visibility contract: `Blake3HashIndex` remains `pub(crate)` and is not leaked through public APIs.

**Acceptance criteria:**
- [ ] `RawPropertyMap::compute_hashes()` returns `Blake3HashIndex<PropertyName>`.
- [ ] No public API exposes `Blake3HashIndex` (it remains `pub(crate)`).
- [ ] Schema delta/property hash behavior is unchanged in targeted tests.

**Out of scope:**
- Renaming any view-layer types.
- Broader config/schema hash API redesign beyond this return-type seam.
