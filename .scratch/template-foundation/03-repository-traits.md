---
title: 03-repository-traits
category: enhancement
label: ready-for-agent
status: completed
branch: 03-repository-traits
merge_commit:
date_created: 2026-06-11
date_completed: 2026-06-12
---

# Template Repository Traits

Status: completed

## Parent

`.scratch/template-foundation/PRD.md`

## What to build

Define the Template repository port: segregated `ReadRepository`, `WriteRepository`, and unified `Repository` traits following the exact pattern established by the note and schema contexts.

The traits cover:
- CRUD operations for `Template` (find by id, find by path/name, save, delete)
- CRUD operations for `RawTemplateView` (find by path, save, delete)
- Batch raw-view operations for efficient multi-path discovery and atomic cache updates

No filesystem materialization belongs here — the traits are pure persistence ports. Both an in-memory test double and a `redb`-backed `RedbRepository` are built in this slice.

## Acceptance criteria

- [x] `ReadRepository` trait defined with at minimum: find `Template` by `TemplateId`, find `Template` by `TemplateName`, list all `Template`s, find `RawTemplateView` by `PathKey`, batch find `RawTemplateView` by a set of `PathKey`s
- [x] `WriteRepository` trait defined with at minimum: save `Template`, delete `Template` by `TemplateId`, save `RawTemplateView`, delete `RawTemplateView` by `PathKey`, batch save `RawTemplateView`s
- [x] `Repository` is a unified blanket marker trait: `impl<T: ReadRepository + WriteRepository> Repository for T {}`
- [x] No filesystem I/O or MiniJinja types appear in any trait method signature
- [x] An in-memory test implementation is provided (or a testing module following `schema/storage/testing.rs` pattern) so downstream slices can write unit tests without a real redb adapter
- [x] Repository contract tests cover: save and find round-trip for `Template`, missing-entity behavior (returns `None` or appropriate error), `RawTemplateView` save/find/delete, batch raw-view operations, find-by-name and find-by-id correctness
- [x] `RedbRepository` struct wraps `Arc<Store>`, constructed via `RedbRepository::new(store)`
- [x] `RedbRepository` implements both `ReadRepository` and `WriteRepository` using `redb` tables
- [x] `save_template` atomically writes template aggregate + name index
- [x] `delete_template` atomically removes template + name index + raw view
- [x] `save_raw_template_view` atomically writes view + path-to-ID index
- [x] All `redb` adapter tests use `tempfile::TempDir` for isolation

## Blocked by

- `issue-01-domain-models.md`

---

> *This was generated by AI during triage.*

## Enhanced Triage — Codebase Analysis (2026-06-12)

**Pattern alignment verified against 3 context repositories (schema, note, vault):**

### Repository Structure

All three contexts follow the same structure — `repository.rs` in the context root, `storage/testing.rs` for test doubles. Template should follow suit:

```
lithos-core/src/template/
├── repository.rs      ← CREATE: ReadRepository + WriteRepository + Repository blanket
├── storage/
│   └── testing.rs     ← CREATE: InMemoryRepository (test double)
```

### Method Naming Consistency

The issue's proposed names largely match project conventions (`docs/naming-taxonomy.md`), but **two naming mismatches** exist:

| Proposed Name | Naming Taxonomy Rule | Suggested Fix |
|---|---|---|
| `save_raw_template_views` | Batch ops use `_many` suffix: `save_many_schemas`, `save_many_base_schemas` | Rename to `save_many_raw_template_views` |
| `delete_raw_template_view` | Singular delete is fine (matches `delete_schema`). ✅ | Keep as-is |

All other names align: `find_*` for optional (returns `Option`), `save_*` for upsert, `delete_*` for removal.

### Error Type Design

The issue proposes `TemplateRepositoryError` with `NotFound` / `Serialization` / `Storage` variants. Cross-context analysis:

| Context | Error Type | Variants |
|---|---|---|
| Schema | `SchemaRepositoryError` | `Storage(DbError)`, `Domain(SchemaError)`, `NotFoundById`, `NotFoundByName`, `NotFoundByPath`, etc. |
| Note | `NoteRepositoryError` | `Storage(DbError)`, `DuplicatePath(NotePath)` |
| Vault | `VaultRepositoryError` | `Storage(DbError)` |

**Recommendation**: Keep `TemplateRepositoryError` simple — `Storage(DbError)` + `NotFoundById(TemplateId)` + `NotFoundByPath(PathKey)`. No separate `Serialization` variant — `DbError::Serialization(String)` already exists at `db/error.rs:47` and any serialization failure is wrapped transitively via `Storage(DbError::Serialization(...))`. Mark `#[non_exhaustive]`. Use `thiserror::Error`. Follow the `SchemaRepositoryError` pattern: `Storage` is the only infra variant; context-specific not-found variants carry the identifier for error reporting.

### In-Memory Test Double

The issue says "follow the `schema/storage/testing.rs` pattern". Key details from that pattern:

1. **Uses `db::testing` primitives**: `InMemoryHarness`, `read_lock`, `write_lock`, `FailurePoint`, `FailureInjector`, `InMemoryDbError`
2. **Module gating**: `#![cfg(any(test, feature = "testing"))]` — both `#[cfg(test)]` and bench/feature use
3. **Thread safety**: All maps behind `Arc<RwLock<HashMap<K, V>>>` — `Clone` is cheap (Arc bump)
4. **Failure injection**: Each method calls `self.harness.fail_at(FailurePoint::BeforeRead|BeforeWrite)?` — templated repo must do the same
5. **Helper methods**: `schema_count()`, `clear()`, `harness()` for test assertions
6. **Doc comments required**: Module doc explains "why in-memory repo exists" and "when to use / not use it" (enforced by `missing_docs = "deny"`)

### Contract Tests

The schema context puts contract tests in `lithos-core/tests/schema_storage.rs` — integration tests that exercise `InMemoryRepository` + `RedbRepository` against the same contract. Template should follow the same dual-implementation test pattern, testing both `InMemoryRepository` and (when built) the redb adapter against a shared contract suite.

### Visibility

- `repository.rs` traits: `pub` (used across contexts — template processor, service)
- `storage/testing.rs` `InMemoryRepository`: `pub(crate)` (only used within `lithos-core`)
- Constructor `InMemoryRepository::new()`: `pub(crate)` to be used by downstream module tests

### Project Constraints from lib.rs

- `unsafe_code = "forbid"` — no unsafe blocks
- `unwrap_used` / `expect_used` denied — use `?` and `match`
- `missing_docs = "deny"` — all `pub` items need doc comments

### Blast Radius Assessment

No downstream consumers yet — this is a greenfield port. LOW risk. The traits will be consumed by:
- Template processor (issue 04)
- Template service (issue 05)
- Integration tests in `lithos-core/tests/template_storage.rs`

## Agent Brief

**Category:** enhancement
**Summary:** Define segregated `ReadRepository`, `WriteRepository`, and `Repository` traits for the template context, plus an in-memory test double

**Current behavior:**
No template persistence port exists. Downstream slices (processor, service) have no trait to program against and no test double to use in unit tests.

**Desired behavior:**
A `lithos-core/src/template/repository.rs` module (mirroring `schema/repository.rs`) defines:

- `ReadRepository` trait with at minimum:
  - `find_template_by_id(&self, id: TemplateId) -> Result<Option<Template>, TemplateRepositoryError>`
  - `find_template_by_name(&self, name: &TemplateName) -> Result<Option<Template>, TemplateRepositoryError>`
  - `list_templates(&self) -> Result<Vec<Template>, TemplateRepositoryError>`
  - `find_raw_template_view(&self, path: &PathKey) -> Result<Option<RawTemplateView>, TemplateRepositoryError>`
  - `find_raw_template_views_by_paths(&self, paths: &[PathKey]) -> Result<Vec<Option<RawTemplateView>>, TemplateRepositoryError>`

- `WriteRepository` trait with at minimum:
  - `save_template(&self, template: &Template) -> Result<(), TemplateRepositoryError>`
  - `delete_template(&self, id: TemplateId) -> Result<(), TemplateRepositoryError>`
  - `save_raw_template_view(&self, view: &RawTemplateView) -> Result<(), TemplateRepositoryError>`
  - `delete_raw_template_view(&self, path: &PathKey) -> Result<(), TemplateRepositoryError>`
  - `save_many_raw_template_views(&self, views: &[RawTemplateView]) -> Result<(), TemplateRepositoryError>`  ⚠️ renamed from `save_raw_template_views` per naming taxonomy

- `Repository` as a unified blanket marker: `impl<T: ReadRepository + WriteRepository> Repository for T {}`

- `TemplateRepositoryError` error type covering at minimum: `Storage(DbError)`, `NotFoundById(TemplateId)`, `NotFoundByPath(PathKey)`. No standalone `Serialization` variant — `DbError::Serialization(String)` already exists and is wrapped transitively via `Storage(DbError::Serialization(...))`. Mark `#[non_exhaustive]`.

- An in-memory test double at `lithos-core/src/template/storage/testing.rs` (mirroring `schema/storage/testing.rs`) usable in unit tests for the processor and service slices. Uses `db::testing::InMemoryHarness` + `read_lock`/`write_lock` + `FailurePoint` — feature-gated `#![cfg(any(test, feature = "testing"))]`.

**Key interfaces:**
- `ReadRepository`, `WriteRepository`, `Repository` — pure trait definitions; no filesystem I/O, no `minijinja` types, no `std::fs`
- `TemplateRepositoryError` — new error enum; at minimum `Storage(DbError)`, `NotFoundById(TemplateId)`, `NotFoundByPath(PathKey)`; no standalone `Serialization` variant (covered by `DbError::Serialization` via `Storage`); `#[non_exhaustive]`; implements `std::error::Error` via `thiserror`
- `InMemoryRepository` (test double) — `Arc<RwLock<HashMap>>`-backed; implements both `ReadRepository` and `WriteRepository`; uses `db::testing::InMemoryHarness` for failure injection; lives in `storage/testing.rs` with `#![cfg(any(test, feature = "testing"))]` gating
- Method signatures mirror the schema repository pattern (qualified names like `find_template_by_id`) per schema convention
- `pub` visibility for `ReadRepository`/`WriteRepository`/`Repository` traits; `pub(crate)` for `InMemoryRepository`
- All `pub` items require doc comments (`missing_docs = "deny"`)
- No `unwrap()`/`expect()` in production code (`unwrap_used`/`expect_used` denied)

**Acceptance criteria:**
- [x] `ReadRepository` trait has find-by-id, find-by-name, list-all, find-raw-view, and batch-find-raw-views methods
- [x] `WriteRepository` trait has save-template, delete-template, save-raw-view, delete-raw-view, and batch-save-raw-views (`save_many_raw_template_views`)
- [x] `Repository` blanket marker trait compiles and is satisfied by any type implementing both `ReadRepository` and `WriteRepository`
- [x] No `std::fs`, `minijinja`, or filesystem I/O in any trait signature or error type
- [x] `TemplateRepositoryError` is defined with `thiserror` and `#[non_exhaustive]`, implements `std::error::Error`; variants: `Storage(DbError)`, `NotFoundById(TemplateId)`, `NotFoundByPath(PathKey)` — no standalone `Serialization` variant
- [x] An in-memory test double is provided in `storage/testing.rs` using `db::testing::InMemoryHarness` + `read_lock`/`write_lock`/`FailurePoint` — gated `#![cfg(any(test, feature = "testing"))]`
- [x] `InMemoryRepository` helper methods: `new()`, `default()`, `harness()`, `template_count()`, `clear()` — all `pub(crate)`
- [x] Failure injection tested: read injection returns `Storage` error, write injection returns `Storage` error
- [x] Repository contract tests pass: save→find round-trip for `Template`, missing-entity returns `None`, `RawTemplateView` save/find/delete, batch raw-view save and find, find-by-name and find-by-id correctness
- [x] `mise run test` passes

**Out of scope:**
- Template processor, service, engine, or CLI
- MiniJinja dependency or Jinja syntax validation
- Frontmatter, query, or extension types

---

## TDD Plan

### File Structure

```
lithos-core/src/template/
├── mod.rs              ← MODIFY: add repository + storage modules, re-exports
├── repository.rs       ← CREATE: TemplateRepositoryError + ReadRepository + WriteRepository + Repository
├── storage/
│   ├── mod.rs          ← CREATE: module declarations + RedbRepository struct + constructor
│   ├── tables.rs       ← CREATE: table constants for redb adapter
│   ├── read.rs         ← CREATE: ReadRepository impl for RedbRepository
│   ├── write.rs        ← CREATE: WriteRepository impl for RedbRepository
│   └── testing.rs      ← CREATE: InMemoryRepository (test double)
```

### Files to Create (6)

| File | Contents |
|------|----------|
| `template/repository.rs` | `TemplateRepositoryError` enum, `ReadRepository` trait (5 methods), `WriteRepository` trait (5 methods), `Repository` blanket marker |
| `template/storage/mod.rs` | Module declarations + `RedbRepository` struct (wraps `Arc<Store>`), `pub fn new(store: Arc<Store>) -> Self` |
| `template/storage/tables.rs` | Table constants: `TEMPLATES` (UuidTable), `TEMPLATE_ID_BY_NAME` (Table), `RAW_TEMPLATE_VIEWS` (PathTable). Uses `impl_redb_uuid!(TemplateId)`. |
| `template/storage/read.rs` | `impl ReadRepository for RedbRepository` — all 5 read methods |
| `template/storage/write.rs` | `impl WriteRepository for RedbRepository` — all 5 write methods |
| `template/storage/testing.rs` | `InMemoryRepository` struct with `InMemoryHarness`, `#[cfg(any(test, feature = "testing"))]` gate, `From<InMemoryDbError>` conversion |

### Files to Modify (1)

| File | Change |
|------|--------|
| `template/mod.rs` | Add `pub(crate) mod repository;`, `pub(crate) mod storage;`. Re-export: `pub use repository::{ReadRepository, WriteRepository, Repository, TemplateRepositoryError};` |

### Red-Green Cycles (Vertical Slices)

#### ✅ Cycle 1: Error type + trait definitions (`repository.rs`)

**RED tests:**
- `TemplateRepositoryError`:
  - Has `Storage(DbError)`, `NotFoundById(TemplateId)`, `NotFoundByPath(PathKey)` variants
  - `#[non_exhaustive]`, derives `thiserror::Error`
  - `From<DbError>` produces `Storage` variant
  - Display messages contain meaningful text
  - `std::error::Error::source()` returns `Some` for `Storage`
- `ReadRepository` trait exists with methods:
  - `find_template_by_id(&self, id: TemplateId) -> Result<Option<Template>, TemplateRepositoryError>`
  - `find_template_by_name(&self, name: &TemplateName) -> Result<Option<Template>, TemplateRepositoryError>`
  - `list_templates(&self) -> Result<Vec<Template>, TemplateRepositoryError>`
  - `find_raw_template_view(&self, path: &PathKey) -> Result<Option<RawTemplateView>, TemplateRepositoryError>`
  - `find_raw_template_views_by_paths(&self, paths: &[PathKey]) -> Result<Vec<Option<RawTemplateView>>, TemplateRepositoryError>`
- `WriteRepository` trait exists with methods:
  - `save_template(&self, template: &Template) -> Result<(), TemplateRepositoryError>`
  - `delete_template(&self, id: TemplateId) -> Result<(), TemplateRepositoryError>`
  - `save_raw_template_view(&self, view: &RawTemplateView) -> Result<(), TemplateRepositoryError>`
  - `delete_raw_template_view(&self, path: &PathKey) -> Result<(), TemplateRepositoryError>`
  - `save_many_raw_template_views(&self, views: &[RawTemplateView]) -> Result<(), TemplateRepositoryError>`
- `Repository` blanket marker: `impl<T: ReadRepository + WriteRepository> Repository for T {}`
- Compiles for a mock struct that `impl ReadRepository + WriteRepository`

**GREEN:** Create `repository.rs` with error enum + trait definitions + blanket impl.

#### ✅ Cycle 2: Module wiring (`mod.rs`)

**RED tests:** (on `template::` imports)
- `template::ReadRepository` is accessible
- `template::WriteRepository` is accessible
- `template::Repository` is accessible
- `template::TemplateRepositoryError` is accessible
- `template::storage::tables` module exists
- `template::storage::testing` module exists (in cfg(test))

**GREEN:** Modify `template/mod.rs` — add `mod repository;`, `mod storage;`, re-exports.

#### ✅ Cycle 3: Storage scaffolding (`storage/mod.rs`, `tables.rs`, `read.rs`, `write.rs`)

**GREEN only (structural):**
- Create `storage/mod.rs` with `pub mod tables;`, `mod read;`, `mod write;`, `pub(crate) mod testing;` (gated)
- Create `storage/tables.rs` with table constants:
  ```rust
  impl_redb_uuid!(TemplateId);
  pub const TEMPLATES: UuidTable<TemplateId, &[u8]> = UuidTable::new("templates");
  pub const TEMPLATE_ID_BY_NAME: Table<&str, &[u8]> = Table::new("template_id_by_name");
  pub const RAW_TEMPLATE_VIEWS: PathTable<&[u8]> = PathTable::new("raw_template_views");
  ```
- Create `storage/read.rs` — module doc explaining deferral to redb slice
- Create `storage/write.rs` — module doc explaining deferral

#### ✅ Cycle 4: InMemoryRepository scaffolding (`storage/testing.rs`)

**RED tests:**
- `InMemoryRepository::new()` returns an instance
- `InMemoryRepository::default()` returns an instance
- `harness()` returns a reference to `InMemoryHarness`
- `template_count()` returns 0 on fresh instance
- Both `new()` and `default()` start at 0 template count
- After `clear()`, template count is 0 (even after saving)
- Operation counters start at 0 reads, 0 writes

**GREEN:** Create `storage/testing.rs` with struct, `new()`, `default()`, `harness()`, `template_count()`, `clear()`. Fields: `harness: Arc<InMemoryHarness>`, `templates: Arc<RwLock<HashMap<TemplateId, Template>>>`, `name_to_id: Arc<RwLock<HashMap<TemplateName, TemplateId>>>`, `raw_views: Arc<RwLock<HashMap<PathKey, RawTemplateView>>>`.

#### ✅ Cycle 5: Template CRUD in InMemoryRepository

**RED tests:**
- **Save round-trip:** Save Template, then `find_template_by_id` returns `Some(template)`
- **Missing entity:** `find_template_by_id` with unknown ID returns `None`
- **Find by name:** Template saved, then `find_template_by_name` returns `Some(template)`
- **Find by name missing:** `find_template_by_name` with unknown name returns `None`
- **List templates:** After saving 2 templates, `list_templates` returns `Vec` of length 2
- **List templates empty:** `list_templates` returns empty `Vec` when none saved
- **Delete template:** After save + delete, `find_template_by_id` returns `None`
- **Delete idempotent:** `delete_template` on missing ID returns `Ok(())`
- **Update (upsert):** Save same `TemplateId` with different name, find returns updated
- **Counters increment:** Each save increments write counter, each find increments read counter

**GREEN:** Implement `ReadRepository` and `WriteRepository` for `InMemoryRepository` — template methods only. Each method calls `self.harness.fail_at(FailurePoint::BeforeRead|BeforeWrite)?`, then `read_lock`/`write_lock` on maps.

#### ✅ Cycle 6: RawTemplateView CRUD + batch

**RED tests:**
- **Save round-trip:** Save RawTemplateView, then `find_raw_template_view` by path returns `Some(view)`
- **Missing view:** `find_raw_template_view` with unknown path returns `None`
- **Delete view:** After save + delete, `find_raw_template_view` returns `None`
- **Delete view idempotent:** Delete on missing path returns `Ok(())`
- **Batch save:** `save_many_raw_template_views` saves N views, all retrievable via `find_raw_template_view`
- **Batch find:** `find_raw_template_views_by_paths` returns correct order, including `None` for missing paths
- **Batch find empty:** Empty slice returns empty `Vec`
- **Update (upsert):** Save same path, find returns updated view
- **Counters:** Save/view operations increment appropriate counters

**GREEN:** Implement remaining `ReadRepository` and `WriteRepository` methods — view methods + batch.

#### ✅ Cycle 7: Failure injection + error conversion

**RED tests:**
- **Read failure (every read method):** `BeforeRead` injection returns `Err(TemplateRepositoryError::Storage(_))` for all 5 read methods
- **Write failure (every write method):** `BeforeWrite` injection returns `Err(TemplateRepositoryError::Storage(_))` for all 5 write methods
- **`From<InMemoryDbError>` conversion:**
  - `LockPoisoned` → `Storage(DbError::Corruption(...))`
  - `InjectedFailure` → `Storage(DbError::Corruption(...))`
  - `InvariantViolation` → `Storage(DbError::Corruption(...))`

**GREEN:** Implement `From<InMemoryDbError> for TemplateRepositoryError` in `testing.rs`. Failure injection works automatically from harness pattern already established in Cycles 5-6.

#### ✅ Cycle 8: Verify (InMemoryRepository)

- `mise run test` passes
- `mise run lint` passes
- `mise run fmt` passes
- No `unwrap()`/`expect()` in production code
- All `pub` items have doc comments

---

### Red-Green Cycles — RedbRepository

#### ✅ Cycle 9: RedbRepository struct + constructor (`storage/mod.rs`)

**RED tests:**

These tests use `tempfile::TempDir` + `Store::open` for isolation (never the real vault).

- `RedbRepository` is constructible from `Arc<Store>`
- `RedbRepository: Send + Sync`
- `RedbRepository: Debug`
- `RedbRepository::new(store)` returns an instance

**GREEN:** Create `storage/mod.rs` with `RedbRepository` struct (wraps `pub(crate) store: Arc<Store>`), constructor `pub fn new(store: Arc<Store>) -> Self`. Module layout: `mod read;`, `mod write;`, `pub mod tables;`. `mod read` and `mod write` are `mod` (not `pub mod`) — implementation details.

#### ✅ Cycle 10: RedbRepository read impl (`storage/read.rs`)

**RED tests:**

Each test creates an isolated redb instance per test method.

- **Find by ID — missing:** `find_template_by_id` returns `Ok(None)` when table is empty
- **Find by ID — found:** After direct `store.write(|tx| ...)` insert, `find_template_by_id` returns `Some(template)`
- **Find by name — missing:** `find_template_by_name` returns `Ok(None)` when name index is empty
- **Find by name — found:** After direct table insert of name→ID index + template aggregate, `find_template_by_name` returns `Some(template)`
- **List — empty:** `list_templates` returns empty `Vec` when no templates exist
- **List — non-empty:** After inserting 2 templates, `list_templates` returns `Vec` of length 2
- **Find raw view by path — missing:** `find_raw_template_view` returns `Ok(None)` when no view exists
- **Find raw view by path — found:** After inserting path index + view, `find_raw_template_view` returns `Some(view)`
- **Batch find views — correct order:** Request 3 paths (middle missing), result order matches input order with `None` for the missing path
- **Batch find views — empty:** Empty path slice returns empty `Vec`

**GREEN:** Implement all 5 `ReadRepository` methods in `storage/read.rs` (`impl ReadRepository for RedbRepository`). Each method opens a read transaction via `self.store.read(|tx| ...)`, uses `tx.try_open_table()` to access tables, and converts errors via `TemplateRepositoryError::from`. Pattern matches `schema/storage/read.rs`.

#### ✅ Cycle 11: RedbRepository write impl (`storage/write.rs`)

**RED tests:**

- **Save template:** `save_template` — after save, `find_template_by_id` returns the template
- **Save template updates name index:** After save, `find_template_by_name` also returns the template
- **Save template rollback on error:** Simulate a serialization error inside a write transaction; original data is unaffected
- **Delete template:** After save + delete, `find_template_by_id` returns `None`
- **Delete template removes name index:** After save + delete, `find_template_by_name` returns `None`
- **Delete template idempotent:** Deleting a missing ID returns `Ok(())`
- **Save raw template view:** After `save_raw_template_view`, `find_raw_template_view` returns the view
- **Save raw template view updates index:** After save, view is findable by path
- **Delete raw template view:** After save + delete, `find_raw_template_view` returns `None`
- **Delete raw template view idempotent:** Deleting a missing path returns `Ok(())`
- **Batch save raw views:** `save_many_raw_template_views` persists all views, all retrievable by path
- **Batch save empty:** Empty slice does not error

**GREEN:** Implement all 5 `WriteRepository` methods in `storage/write.rs` (`impl WriteRepository for RedbRepository`). Each method opens a write transaction via `self.store.write(|tx| ...)`. `save_template` atomically writes to both `TEMPLATES` and `TEMPLATE_ID_BY_NAME`. `delete_template` removes from `TEMPLATES`, `TEMPLATE_ID_BY_NAME`, and `RAW_TEMPLATE_VIEWS`. Uses `to_bytes()` for serialization. Pattern matches `schema/storage/write.rs`.

#### ✅ Cycle 12: Verify

- `mise run test` passes (all InMemoryRepository + RedbRepository tests)
- `mise run lint` passes
- `mise run fmt` passes
- No `unwrap()`/`expect()` in production code (only in tests)
- All `pub` items have doc comments
- `RedbRepository` constructor, `ReadRepository` impl, and `WriteRepository` impl all have doc comments

### Test Organization

InMemoryRepository tests live in `storage/testing.rs` (unit tests, no redb needed). RedbRepository tests live in `storage/read.rs` and `storage/write.rs` (integration tests with temp redb instances). Following the schema pattern in `testing.rs`:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    mod fixtures {
        // helper functions: test_template(), test_view()
    }

    mod defaults {
        // new(), default(), counters
    }

    mod accessors {
        // harness(), template_count(), clear()
    }

    mod template {
        mod save { ... }
        mod lookup { ... }
        mod delete { ... }
    }

    mod raw_view {
        mod save { ... }
        mod lookup { ... }
        mod delete { ... }
        mod batch { ... }
    }

    mod failure_injection {
        mod before_read { ... }
        mod before_write { ... }
    }

    mod error_conversion {
        mod in_memory_db_error { ... }
    }
}
```

### Behaviors to Verify

1. Error type has correct variants and derives
2. Trait definitions match interface contracts
3. Module re-exports work correctly
4. Table constants are defined for redb adapter
5. InMemoryRepository constructs and reports zero state
6. Template save→find round-trip (by id, by name, list)
7. Missing template returns None
8. Template delete (with idempotency)
9. Template upsert (save overwrites existing)
10. RawTemplateView save→find round-trip (by path, batch)
11. Missing view returns None
12. View delete (with idempotency)
13. Batch save + batch find consistency
14. Failure injection on all read methods (BeforeRead → Storage)
15. Failure injection on all write methods (BeforeWrite → Storage)
16. Operation counter accuracy
17. InMemoryDbError → TemplateRepositoryError conversion
18. RedbRepository constructible from `Arc<Store>`, implements `Send + Sync + Debug`
19. RedbRepository template save→find round-trip (by id, by name)
20. RedbRepository template delete removes aggregate + name index
21. RedbRepository raw view save→find round-trip (by path)
22. RedbRepository batch find raw views preserves order
23. RedbRepository save rollback on error (transactional integrity)
24. RedbRepository batch save raw views persists all

### Dependencies

| Dependency | Source | Used For |
|-----------|--------|----------|
| `crate::db::DbError` | `lithos-core/src/db/error.rs` | `TemplateRepositoryError::Storage` |
| `crate::db::testing::*` | `lithos-core/src/db/testing.rs` | `InMemoryHarness`, `read_lock`, `write_lock`, `FailurePoint`, `InMemoryDbError` |
| `crate::fs::PathKey` | `lithos-core/src/fs/` | Raw template view path lookup |
| `crate::template::aggregate::*` | `lithos-core/src/template/aggregate.rs` | `Template`, `TemplateId`, `TemplateName` |
| `crate::template::views::RawTemplateView` | `lithos-core/src/template/views.rs` | Cache/freshness view |
| `crate::db::{UuidTable, Table, PathTable}` | `lithos-core/src/db/` | Table constants in `tables.rs` |
| `crate::impl_redb_uuid` | `lithos-core/src/db/` | UUID table key impl |
| `crate::db::Store` | `lithos-core/src/db/core.rs` | RedbRepository database handle |
| `redb` | `Cargo.toml` (workspace) | Storage engine |
| `tempfile` | `Cargo.toml` (dev-dependency) | Isolated temp dirs for redb tests |

### Risk Assessment: LOW

- No existing downstream consumers (greenfield port)
- Pattern replicated from 3 existing, well-tested contexts
- RedbRepository follows exact schema context pattern (read.rs:69, write.rs:64)
- No `unsafe`, no MiniJinja

### Notes

- `storage/read.rs` and `storage/write.rs` implement the `ReadRepository` and `WriteRepository` traits for `RedbRepository` using `redb` tables. Implementation pattern follows `schema/storage/read.rs` and `schema/storage/write.rs` exactly.
- Table constants in `tables.rs` define the redb table contracts: `TEMPLATES` (UuidTable for Template aggregates), `TEMPLATE_ID_BY_NAME` (str→bytes index for name lookups), `RAW_TEMPLATE_VIEWS` (PathTable for raw template views).
- Template read methods are simpler than schema (no inheritance graph, property bank, or versioned views).
- Template write methods are simpler than schema (no property bank, no base schemas, no inheritance persistence).
- `delete_template` atomically removes the template aggregate, name index, and raw view in one transaction (matching schema `delete_schema` pattern).
- PRD says `RawTemplateView` should follow "version-history pattern of `RawSchemaView`" but implementation (views.rs:30) chose "flat struct — NOT a versioned ring buffer". This drift is correct for templates (no inheritance metadata) and has no effect on this task.

---

## Completion Summary (2026-06-12)

All 12 TDD cycles completed. 1,832 tests pass, clippy clean, fmt clean.

### Code Review Deviation

During code review, `TemplateRepositoryError` was **moved from `repository.rs` to `error.rs`**
(committed at `7f7d2c5c`). Rationale:

- Follows the schema context pattern where `SchemaRepositoryError` lives in `schema/error.rs`,
  colocated with `SchemaError`, not in `schema/repository.rs`.
- Keeps all error types for the template context in one file (`error.rs`), matching the
  module doc's promise of "domain-level error types for template validation and persistence".
- `TemplateError` gained `Repository(#[from] TemplateRepositoryError)` and lost
  `Clone + PartialEq + Eq` (incompatible with `DbError` which is neither `Clone` nor
  `PartialEq`).

### Remaining Work

- Pre-existing `config/builder.rs` compile errors (fields moved under `.paths.` in the
  config restructure) need fixing separately.
