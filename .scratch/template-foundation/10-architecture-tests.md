---
title: 09-architecture-tests
category: enhancement
label: wontfix
status: closed
branch:
merge_commit:
date_created: 2026-06-11
date_completed:
---

# Architecture Tests: Template Context Isolation

Status: ready-for-agent

## Parent

`.scratch/template-foundation/PRD.md`

## What to build

Extend the existing architecture tests in `lithos-core/tests/architecture.rs` to enforce Template context isolation boundaries. The existing tests already cover note/schema cross-imports and FS isolation — this slice adds parallel coverage for the template context.

Rules to enforce:

1. **No cross-context imports** — `template` context must not import `crate::note` or `crate::schema`. The existing `contexts_must_not_import_each_other` test should be extended or a new test added to cover `template` as a participant.

2. **MiniJinja confined to adapter** — `minijinja` must not appear in template domain models, repository traits, service request/response types, or `TemplateError`. It is only permitted inside the `template/engine/` adapter module (or equivalent adapter boundary). A new architecture test should enforce this by scanning non-adapter template source files for `minijinja` imports.

3. **No raw `std::fs` in template use cases** — template service, processor, and artifact pipeline must not import `std::fs` directly. The existing `ports_must_not_import_std_fs` test may already cover this if template modules are included in its scope; verify and extend if not.

Prior art: the existing tests in `lithos-core/tests/architecture.rs` use source-file scanning (reading `.rs` files and checking for forbidden import patterns). Follow the same approach.

## Acceptance criteria

- [ ] `contexts_must_not_import_each_other` test (or a new companion test) covers `template` context: `template` does not import `crate::note` or `crate::schema`
- [ ] A test enforces that `minijinja` does not appear in template source files outside the designated adapter module
- [ ] The FS isolation test covers template modules (no raw `std::fs` in template service, processor, or artifact pipeline)
- [ ] All three tests pass after the implementation slices (1–8) are complete
- [ ] No false positives: the adapter module itself is correctly excluded from the MiniJinja confinement check

## Blocked by

- `issue-05-engine-port-adapter.md` (defines the adapter boundary that the MiniJinja confinement test must reference)
- `issue-07-template-service.md` (provides the full module tree the tests scan)

---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Extend `lithos-core/tests/architecture.rs` to enforce template context isolation, MiniJinja confinement, and FS isolation

**Current behavior:**
`lithos-core/tests/architecture.rs` already enforces:
- `contexts_must_not_import_each_other` for `["config", "note", "schema", "template"]` — `template` is already in the list but there is no `template` context module yet, so the test is a no-op for it today
- `ports_must_not_import_std_fs` — scans `src/**/ports.rs` files for `std::fs`
- Various other config/discovery boundary rules

After the implementation slices (issues 01–08) complete, three rules need to be actively verified by tests:

**Desired behavior:**

**Rule 1 — No cross-context imports** (already partially covered):
The `contexts_must_not_import_each_other` test already lists `"template"` in the `contexts` array. Verify this test will correctly catch any `crate::note` or `crate::schema` import from a `template` context file. If the glob pattern `src/template/**/*.rs` returns no files today, the test silently passes — confirm it will correctly fire once the template module exists. No code change needed if the existing test already covers it; document what was verified.

**Rule 2 — MiniJinja confined to adapter module** (new test):
Add a new test `minijinja_must_be_confined_to_engine_adapter` that:
1. Globs all `src/template/**/*.rs` files
2. **Excludes** the adapter module path (e.g. files matching `src/template/engine/minijinja.rs` or `src/template/engine/` — the exact path determined by issue-05's implementation)
3. Asserts that none of the remaining files contain `"minijinja"` as an import or use path component
4. Produces a clear failure message naming the offending file if the assertion fails

**Rule 3 — No raw `std::fs` in template use cases** (extend existing or verify scope):
The existing `ports_must_not_import_std_fs` test scans `src/**/ports.rs` only. Template use cases (service, processor, artifact pipeline) may not live in files named `ports.rs`. Add a new test `template_use_cases_must_not_import_std_fs` that:
1. Globs `src/template/**/*.rs` files
2. Excludes the MiniJinja adapter module (same exclusion as Rule 2)
3. Asserts no file contains `use std::fs` or `std::fs::` as a direct import
4. Produces a clear failure message naming the offending file

All three tests must pass with the template implementation in place and must produce no false positives against the adapter module.

**Key interfaces:**
- `lithos-core/tests/architecture.rs` — the file to modify; follow the existing source-scanning style (read file content as string, assert `!content.contains(...)`)
- `glob` crate — already used in the test file; use the same pattern
- Adapter module path — determined by issue-05 implementation; the test must use the same path string the implementation chooses (e.g. `src/template/engine/minijinja.rs`); coordinate with the issue-05 agent or inspect the completed implementation before writing the exclusion pattern

**Acceptance criteria:**
- [ ] `contexts_must_not_import_each_other` correctly scans `src/template/**/*.rs` files and would catch `crate::note` or `crate::schema` imports; verified by inspection or a negative test
- [ ] A new `minijinja_must_be_confined_to_engine_adapter` test is present and passes
- [ ] The confinement test correctly excludes the adapter module path from the `minijinja` check
- [ ] A new `template_use_cases_must_not_import_std_fs` test (or equivalent extension) is present and passes
- [ ] The FS isolation test correctly scans template service, processor, and artifact pipeline files
- [ ] All three tests produce clear, actionable failure messages that name the offending file
- [ ] No false positives: the MiniJinja adapter module passes both exclusions correctly
- [ ] All architecture tests pass after issues 01–08 are implemented (`mise run test` in `lithos-core`)

**Out of scope:**
- Changes to any template implementation file (issues 01–08)
- Architecture rules for contexts other than `template`
- Linting or clippy-based enforcement (source scanning only, consistent with existing test style)
- Runtime enforcement or proc macros

---

> *This was generated by AI during triage.*

## Triage Resolution

**Closed as `wontfix`.**

The workspace was moved to a `crates/` architecture. This fundamentally solved the isolation constraints natively through `Cargo.toml`.

- **Rule 1 (Cross-context):** Handled natively by Cargo crate isolation + existing `contexts_must_not_import_each_other` in `crates/app/tests/architecture.rs`.
- **Rule 2 (MiniJinja confinement):** Already fully implemented natively inside `crates/template/src/lib.rs` (via the policy tests `no_rendering_engine_import_in_template_context` and `rendering_engine_imports_are_confined_to_engine_adapter`).
- **Rule 3 (FS isolation):** `std::fs` usage in production code is already virtually non-existent, making a strict string-matching text scan overkill for this specific case.

If we start seeing `std::fs` leaking into domain services and a global clippy lint (`clippy::disallowed_methods`) isn't sufficient, we can revisit.
