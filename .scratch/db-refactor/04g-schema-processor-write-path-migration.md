---
title: 04g-schema-processor-write-path-migration
category: enhancement
label: ready-for-agent
status: completed
date_created: 2026-05-13
date_completed: 2026-05-13
---

## Type

AFK

## Parent

- `04-complete-schema-adapter-migration.md`

## What to build

Migrate schema write orchestration in `schema_processor` from legacy write calls
to the v2 write seam, including save-many, raw view persistence, topological
graph save, and delete operations used during completion.

This slice is complete when processor write paths run end-to-end on the v2 seam
with equivalent atomic behavior.

## Acceptance Criteria

- [x] `schema_processor` no longer calls legacy `save_schemas(&[&Schema])`.
- [x] Save-many, delete, raw-view, and topology writes used by processor are
      routed through v2 write methods.
- [x] Atomic semantics are preserved for processor-triggered persistence
      operations.
- [x] Existing processor tests pass and no behavior regressions are introduced.
- [x] No clippy warnings and code is formatted.

## Blocked by

- `04f-builder-discovery-seam-migration.md`

## Notes

- Keep this slice focused on write-path cutover and behavior parity.

## Triage Notes (2026-05-13)

> *This was generated by AI during triage.*

- Recommendation: keep `enhancement` + `needs-triage` until maintainer confirms
  readiness, then move to `ready-for-agent`.
- Refactoring boundary is clear: this slice owns write orchestration cutover in
  `schema_processor` and should not absorb batch-read abstraction redesign.
- Primary legacy callsites to migrate are `save_schemas` usage in:
  - `lithos-core/src/schema/schema_processor.rs:2774`
  - `lithos-core/src/schema/schema_processor.rs:2823`
- Preserve atomicity for save/delete/raw-view/topology effects while aligning
  with v2 write API semantics.

## Agent Brief

> *This was generated by AI during triage.*

Implement issue `04g-schema-processor-write-path-migration` via strict TDD
vertical slices.

### Scope

- Migrate schema processor write orchestration from legacy write seam methods to
  v2 write methods.
- Preserve behavior and transaction invariants.

### Required outcomes

1. `schema_processor` no longer depends on legacy `save_schemas(&[&Schema])`.
2. Processor write flow uses v2-compatible write methods for:
   - save-many schema persistence
   - raw schema view persistence
   - delete operations
   - topological graph persistence
3. Observable loader/processor behavior remains unchanged.

### File focus

- `lithos-core/src/schema/schema_processor.rs`
- `lithos-core/src/schema/storage.rs` (only where needed for compatibility)
- `lithos-core/src/schema/repository.rs`
- `lithos-core/src/schema/storage_v2/write.rs`

### TDD slices

1. RED: fail on processor completion path that still requires legacy save-many.
2. GREEN: introduce minimal adapter/wiring to route save-many via target seam.
3. RED: fail on delete + topology persistence parity in completion flow.
4. GREEN: route writes and pass.
5. REFACTOR: remove transitional duplication while keeping all tests green.

### Verification

- Run focused schema processor tests.
- Run `mise run fmt`.
- Run `mise run lint`.
- Run `mise run test`.

## Pre-Implementation Update (2026-05-13)

> *This was generated by AI during triage.*

- GitNexus impact analysis (upstream) was run on processor write-path symbols
  before implementation:
  - `construct_new_schemas` (`lithos-core/src/schema/schema_processor.rs`)
  - `complete` (`lithos-core/src/schema/schema_processor.rs`)
- Blast radius summary for both symbols:
  - Risk: `LOW`
  - Direct callers: `1` (`Builder::load_all`)
  - Primary affected process: `builder_load_all_orchestrates_discovery`
  - Main affected area: schema loader tests under
    `lithos-core/tests/schema_loader.rs`
- No HIGH/CRITICAL warning, so this slice is safe to execute as planned.

### TDD execution plan (vertical slices)

1. RED: add/adjust a failing processor-path test proving legacy
   `save_schemas(&[&Schema])` dependency is still required on completion/new-build
   path.
2. GREEN: introduce minimal write seam in repository traits for processor writes
   and route processor save-many to the v2 write method (`save_many_schemas`).
3. RED: add failing parity test for delete + topology persistence in completion
   flow after seam routing.
4. GREEN: route delete/topology write calls through the same seam and restore
   behavior parity.
5. REFACTOR: remove transitional duplication and keep public behavior/tests
   unchanged.

## Implementation Notes (2026-05-13)

- Added `ProcessorWriteRepository` seam to isolate schema processor write-path
  persistence from legacy repository coupling.
- Implemented dual wiring for migration safety:
  - blanket implementation for legacy `storage::Repository`
  - concrete implementation for v2 `SchemaRedbRepository`
- Migrated processor write callsites in
  `lithos-core/src/schema/schema_processor.rs`:
  - `construct_new_schemas` now persists via `save_many_schemas(&[Schema])`
  - `complete` now persists via `save_many_schemas(&[Schema])`
  - completion delete/topology writes remain routed via seam methods
- Added v2 seam coverage test in
  `lithos-core/src/schema/storage_v2/write.rs`:
  - `processor_write_seam_supports_batch_persistence`

## Verification Notes (2026-05-13)

- `mise run fmt` passed.
- `mise run lint` passed.
- `mise run test:unit` passed (`1128` passed, `0` failed).
- `mise run test` passed:
  - unit summary: `1128` passed, `0` failed
  - integration summary: `36` passed, `0` failed
  - e2e summary: `1` passed, `0` failed
- `gitnexus_detect_changes(scope: "all")` reported no indexed symbol/process
  deltas in current graph snapshot (`risk_level: low`, `changed_files: 10`).
