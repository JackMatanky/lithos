---
title: 04h-batch-read-compat-migration
category: enhancement
label: needs-triage
status: open
date_created: 2026-05-13
---

## Type

AFK

## Parent

- `04-complete-schema-adapter-migration.md`

## What to build

Replace legacy batch-read coupling (`with_batch_schema_reader` and related
legacy shapes) with v2-compatible read composition while preserving efficient,
single-pass discovery behavior.

This slice is complete when batch-style schema reads needed by runtime
orchestration no longer depend on legacy storage traits.

## Acceptance Criteria

- [ ] Runtime schema flows no longer depend on legacy
      `with_batch_schema_reader` APIs.
- [ ] Path->ID and path->RawSchemaView lookup behavior remains correct for
      mixed hit/miss input sets.
- [ ] Any required v2-side helper abstraction is minimal and stays within the
      schema context boundaries.
- [ ] Integration and regression tests covering discovery/refresh behavior
      continue to pass.
- [ ] No clippy warnings and code is formatted.

## Blocked by

- `04g-schema-processor-write-path-migration.md`

## Notes

- Prioritize behavior parity and performance characteristics over API elegance.

## Triage Notes (2026-05-13)

> *This was generated by AI during triage.*

- Recommendation: keep `enhancement` + `needs-triage` until maintainer confirms
  readiness, then move to `ready-for-agent`.
- This slice should specifically remove runtime dependency on legacy
  `with_batch_schema_reader` coupling while preserving one-pass lookup behavior
  for discovery.
- Keep this issue separate from write-path migration and rename cleanup.
- Paths most affected:
  - `lithos-core/src/schema/discovery.rs`
  - `lithos-core/src/schema/storage.rs`
  - v2 read-side support in `lithos-core/src/schema/storage_v2/read.rs`

## Agent Brief

> *This was generated by AI during triage.*

Implement issue `04h-batch-read-compat-migration` with strict TDD vertical
slices and explicit parity checks for mixed hit/miss lookups.

### Scope

- Replace runtime dependency on legacy batch-schema reader APIs with v2
  compatible read composition.
- Preserve efficient discovery behavior (single coherent read pass semantics).

### Required outcomes

1. Runtime schema flows no longer invoke legacy `with_batch_schema_reader`.
2. Discovery still correctly resolves mixed hit/miss sets for:
   - path -> schema id
   - path -> raw schema view
3. Any compatibility abstraction remains minimal and bounded to schema context.

### File focus

- `lithos-core/src/schema/discovery.rs`
- `lithos-core/src/schema/storage.rs`
- `lithos-core/src/schema/repository.rs`
- `lithos-core/src/schema/storage_v2/read.rs`

### TDD slices

1. RED: add failing discovery test for mixed hit/miss path-set behavior under
   target read composition.
2. GREEN: implement minimal read composition layer and pass.
3. RED: add failing regression for graph/property-bank view retrieval in same
   cached-state assembly.
4. GREEN: restore parity and pass.
5. REFACTOR: trim temporary glue while preserving test coverage.

### Verification

- Run focused discovery + schema read tests.
- Run `mise run fmt`.
- Run `mise run lint`.
- Run `mise run test`.
