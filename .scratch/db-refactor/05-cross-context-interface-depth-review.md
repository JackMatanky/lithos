---
title: 05-cross-context-interface-depth-review
category: enhancement
label: ready-for-human
status: open
date_created: 2026-05-10
---

## Type

HITL

## Labels

- ready-for-human

## What to build

Run an architecture review checkpoint after Schema migration to verify that reducing DB Module Interface width did not make context Repository interfaces shallow or overloaded. Evaluate seam quality, locality, and leverage across Schema, Note, Template, and Config contexts.

This slice is complete when decisions are recorded for any required interface splits or adjustments before broader context rollout.

> *This was generated by AI during triage.*

## Triage Brief

**Category:** enhancement
**Status:** `ready-for-human`

**Summary:** Evaluate context Repository interfaces (Read/Write/Unified) after DB seam changes to ensure cohesion and depth, and approve rollout constraints.

**Current behavior:**
ADR 016 split the monolithic `Repository` trait into a Segregated Unified Repository pattern (`ReadRepository`, `WriteRepository`, and `Repository`). The Schema context has migrated to this pattern, but it must be verified whether the resulting interfaces remain cohesive and deep, or if they have become shallow or overloaded, before rolling out to Note, Template, and Config contexts.

**Desired behavior:**
A human architect reviews the Schema context's new repository traits and storage implementations to evaluate seam quality, locality, and leverage. The review determines if the interfaces need adjustment or if the pattern is ready for broader rollout across Note, Template, and Config contexts. Decisions must be recorded.

**Why this requires a human:**
Evaluating "seam quality, locality, and leverage" requires subjective architectural judgment, domain knowledge, and qualitative assessment of code maintainability that cannot be effectively delegated to an AFK agent.

**Key interfaces:**
- `SchemaReadRepository`, `SchemaWriteRepository`, `SchemaRepository` traits
- `SchemaRedbRepository` implementation and its `read.rs` / `write.rs` split
- DB Module interface boundaries

**Acceptance criteria:**
- [ ] Review documents whether context Repository interfaces remain cohesive and deep after DB seam changes.
- [ ] Any required seam changes (split/merge) are explicitly decided and recorded.
- [ ] Rollout constraints for Note/Template/Config are approved for AFK execution.

**Out of scope:**
- Actually implementing the migration for Note, Template, or Config contexts (this issue is just the review checkpoint).

## Blocked by
- `04-complete-schema-adapter-migration.md`
