---
title: 04f-builder-discovery-seam-migration
category: enhancement
label: needs-triage
status: open
date_created: 2026-05-13
---

## Type

AFK

## Parent

- `04-complete-schema-adapter-migration.md`

## What to build

Migrate the runtime schema orchestration seam for `Builder` and `DiscoveryEngine`
from the legacy `schema::storage::Repository` API to the v2 segregated
repository seam (`SchemaReadRepository` + `SchemaWriteRepository`) while keeping
schema discovery behavior unchanged.

This slice is complete when discovery and builder flows read cached schema state
through the v2 seam end-to-end, without introducing behavior drift.

## Acceptance Criteria

- [ ] `schema::builder` and `schema::discovery` no longer require
      `schema::storage::Repository` for schema reads.
- [ ] Discovery still performs one coherent cached-state read pass for graph,
      property bank view, raw schema views, and schema IDs.
- [ ] Existing schema loader/discovery tests pass without changing user-visible
      behavior.
- [ ] No clippy warnings and code is formatted.

## Blocked by

- `04e-remaining-schema-operations.md`

## Notes

- Keep this issue focused on seam migration and behavior parity.
- Do not mix in module renaming or legacy module deletion here.

## Triage Notes (2026-05-13)

> *This was generated by AI during triage.*

- Recommendation: keep `enhancement` + `needs-triage` until maintainer confirms
  readiness, then move to `ready-for-agent`.
- Refactoring analysis shows this slice should stay read-oriented and avoid
  mixing schema-processor write-path migration to keep vertical scope stable.
- Relevant runtime call paths are in:
  - `lithos-core/src/schema/builder.rs`
  - `lithos-core/src/schema/discovery.rs`
  - `lithos-core/src/schema/storage.rs` (legacy seam currently imported)
- Keep behavioral contract unchanged: single coherent cached-state retrieval
  for graph, property-bank view, path->id, and path->raw-view lookups.

## Agent Brief

> *This was generated by AI during triage.*

Implement issue `04f-builder-discovery-seam-migration` using strict TDD vertical
slices, with behavior parity as the primary constraint.

### Scope

- Migrate `Builder` and `DiscoveryEngine` read-side seam usage away from direct
  runtime dependency on legacy `schema::storage::Repository` where feasible
  within this slice.
- Keep write-path cutover and module renaming out of scope (covered by later
  issues).

### Required outcomes

1. Builder/discovery read orchestration references v2 read abstractions instead
   of legacy storage-seam-only read hooks where possible in this phase.
2. Discovery still collects cached state in one coherent pass:
   - topological graph
   - raw property bank view
   - raw schema views by paths
   - schema IDs by paths
3. Existing user-visible schema loading behavior remains unchanged.

### File focus

- `lithos-core/src/schema/builder.rs`
- `lithos-core/src/schema/discovery.rs`
- `lithos-core/src/schema/repository.rs`
- `lithos-core/src/schema/storage_v2/read.rs`
- Tests in schema discovery/builder pathways

### TDD slices

1. RED: add/adjust one discovery test that fails when required cached state is
   not sourced through the intended read seam.
2. GREEN: implement minimal seam-routing change to pass.
3. RED: add regression for mixed hit/miss path lookups in discovery assembly.
4. GREEN: keep one-pass lookup semantics and pass.
5. REFACTOR: simplify wiring without changing behavior.

### Verification

- Run focused schema discovery/builder tests.
- Run `mise run fmt`.
- Run `mise run lint`.
- Run `mise run test`.
