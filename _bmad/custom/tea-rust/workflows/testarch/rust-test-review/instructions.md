# Rust Test Review Workflow

**Workflow:** `tea-rust-test-review`
**Version:** 1.0 (Step-File Architecture)

## Overview
Perform senior-dev test review: identify issues, apply fixes in code when possible, and produce actionable report.

## INITIALIZATION SEQUENCE
1. Load config and variables from `workflow.yaml`
2. Execute `steps-c/step-01-load-context.md`
