# Rust Unit Test Workflow

**Workflow:** `tea-rust-unit`
**Version:** 1.0 (Step-File Architecture)

## Overview
Generate comprehensive unit tests for existing Rust code with risk-based prioritization, adaptive scope expansion, and GWT comments.

## INITIALIZATION SEQUENCE

1. Load config and resolve variables from `workflow.yaml`
2. Load and execute: `steps-c/step-01-load-context.md`
