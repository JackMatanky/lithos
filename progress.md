# Progress Log

- Initialized planning files for shared-type analysis task.
- Searched for `*Id` wrappers and UUID usage across `lithos-core/src`.
- Confirmed 5 UUID-backed ID wrappers with near-identical behavior and boilerplate.
- Reviewed representative files to compare differences in API surface.
- Identified secondary repetition in `*Name(Box<str>)` types with divergent validation semantics.
- Re-indexed GitNexus (index was 2 commits behind) before DB exploration.
- Audited UUID-specific DB APIs in `db/reader.rs` and `db/writer.rs`, including batch/UoW variants.
- Identified DB-level improvement opportunities for a primary `UuidV7` type (invariant enforcement + API consistency).
- Drafted concrete `UuidV7` type API, DB signature migration, helper dedup strategy, and phased rollout in planning files.
- Added implementation-ready Step 1 spec for `lithos-core/src/support/uuid.rs` with API, error contract, wiring, and test checklist.
- Implemented Step 1: added `UuidV7` and `UuidV7Error` in `lithos-core/src/support/uuid.rs`, exported module in `support/mod.rs`, and added 8 passing unit tests.
- Ran `cargo test -p lithos-core support::uuid::tests` (all green) and `cargo fmt --all`.
- Ran impact analysis before edits: `SchemaId` and `PropertyId` reported LOW risk, no direct upstream dependents in graph output.
- Pilot migration complete: `SchemaId` and `PropertyId` now wrap `UuidV7` internally and expose `as_uuid_v7()`.
- Added tests `schema_id_exposes_uuid_v7_view` and `property_id_exposes_uuid_v7_view`; both pass.
- Started Step 4: DB UUID API migration (`uuid::Uuid` -> `UuidV7`) with planning files updated live.
- Updated DB API signatures in `db/reader.rs` and `db/writer.rs` so `*_by_uuid` methods now take `UuidV7`.
- Migrated immediate call sites in `schema/storage.rs`, `note/storage.rs`, and `template/adapter/command.rs` to pass `UuidV7`.
- `cargo check -p lithos-core` now succeeds after Step 4 signature and call-site updates.
- Updated DB unit-test call sites in `db/reader.rs` and `db/writer.rs` to pass `UuidV7`.
- Validation: `schema_storage` integration tests pass (10/10), note storage targeted tests pass (2/2), and template adapter targeted run reports no failures.
- Marked Step 4 complete in `task_plan.md`.
- Started Step 5: migrating remaining ID wrappers (`NoteId`, `ListItemId`, `VaultId`, `TemplateId`).
- Completed Step 5 wrapper migrations and template ID refactor; compile and targeted tests are green.
- Started Step 6: cleanup remaining temporary UUID bridges and run full verification.
- First `mise run verify` attempt failed during lint because benchmark call sites still passed `Uuid` to `put_by_uuid`/`delete_by_uuid` (now `UuidV7`).
- Updated benchmark call sites in `lithos-core/benches/string_construction.rs`, `lithos-core/benches/db_storage.rs`, and `lithos-core/benches/db_key_handling.rs` to pass `UuidV7`.
- Ran `mise run lint` after fixes: pass.
- Ran `mise run verify` after fixes: pass (985/985 unit tests, 36/36 integration tests, doctests passed).
- Marked Step 6 complete in `task_plan.md`.
